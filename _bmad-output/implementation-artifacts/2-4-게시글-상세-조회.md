# Story 2.4: 게시글 상세 조회

Status: done

## Story

As a 사용자,
I want to view the full details of a post including all photos, seller info, and manner temperature,
so that I can decide whether to contact the seller.

## Acceptance Criteria

1. `GET /api/v1/posts/{post_id}`를 호출하면 `{photos[], title, description, price, is_free, status, trade_place, view_count, chat_count, like_count, seller_email, manner_temp}`가 반환된다 (인증 불필요)
2. 조회 시마다 `view_count`가 1 증가한다
3. 존재하지 않거나 `is_draft=True`인 게시글 조회 시 404(`POST_NOT_FOUND`)가 반환된다
4. Next.js `/posts/[id]` 페이지에서 ImageSlider가 상단에 전체 너비로 렌더링된다 (가로 스와이프, 하단 인디케이터 점)
5. Next.js 판매자 프로필(seller_email)과 MannerTempWidget(manner_temp)이 표시된다
6. Next.js 판매자 본인이면 "수정" / "삭제" 버튼, 타인(비로그인 포함)이면 "채팅하기" 버튼이 하단에 고정 표시된다
7. Flutter 상세 화면에서 동일한 레이아웃과 역할 분기 버튼이 표시된다
8. Flutter 홈 피드에서 PostCard 탭 시 상세 화면으로 이동한다

## Tasks / Subtasks

- [x] Task 1: carrot-api — PostDetailResponse 스키마 확장 (AC: #1)
  - [x] `app/features/posts/schemas.py`의 `PostDetailResponse`에 chat_count=0, like_count=0, manner_temp=36.5 추가
  - [x] 기존 `create_post` 서비스가 명시적 생성으로 변경

- [x] Task 2: carrot-api — get_post_detail repository (AC: #1, #2, #3)
  - [x] `get_post_detail()`: Post + User.manner_temp JOIN + like/chat 서브쿼리 단일 쿼리
  - [x] `increment_view_count()`: 원자적 UPDATE

- [x] Task 3: carrot-api — get_post_detail service (AC: #1, #2, #3)
  - [x] None → POST_NOT_FOUND(404), view_count+1 포함 PostDetailResponse 반환

- [x] Task 4: carrot-api — GET /{post_id} 라우터 (AC: #1, #2, #3)
  - [x] Path(ge=1), 인증 불필요, response_model=PostDetailResponse

- [x] Task 5: carrot-api — 단위 테스트 2개 (26/26 통과)

- [x] Task 6: carrot-web — posts.ts PostDetailResponse 확장 + getPost() 추가

- [x] Task 7: carrot-web — `/posts/[id]/page.tsx` 신규 생성 (ImageSlider, 프로필, 버튼 분기)

- [x] Task 8: carrot-mobile — PostsRepository.getPost() 추가

- [x] Task 9: carrot-mobile — PostDetailScreen 신규 생성 (PageView 슬라이더, 소유자 분기)

- [x] Task 10: carrot-mobile — FeedScreen PostCard onTap → PostDetailScreen 연결

## Dev Notes

### PostDetailResponse 확장 방향

기존 `PostDetailResponse`는 `model_validate(post)` (ORM→Pydantic) 방식을 사용했음.
`chat_count`, `like_count`, `manner_temp`는 Post ORM 모델에 없으므로 **명시적 생성**으로 전환:

```python
# service.py create_post (기존) — model_validate 제거
return PostDetailResponse(
    id=post.id,
    seller_email=post.seller_email,
    title=post.title,
    description=post.description,
    price=post.price,
    is_free=post.is_free,
    status=post.status,
    trade_place=post.trade_place,
    photos=post.photos,
    view_count=post.view_count,
    created_at=post.created_at,
    updated_at=post.updated_at,
    chat_count=0,      # 새 게시글은 채팅 없음
    like_count=0,      # 새 게시글은 찜 없음
    manner_temp=36.5,  # 기본값 (판매자 정보 불필요)
)
```

### carrot-api: repository get_post_detail 패턴

```python
# app/features/posts/repository.py 추가

from app.features.users.models import User
from sqlalchemy import update

async def get_post_detail(
    db: AsyncSession,
    post_id: int,
) -> tuple[Post, int, int, float] | None:
    like_count_sq = (
        select(func.count(Like.id))
        .where(Like.post_id == Post.id)
        .correlate(Post)
        .scalar_subquery()
    )
    chat_count_sq = (
        select(func.count(ChatMessage.id))
        .where(ChatMessage.post_id == Post.id)
        .correlate(Post)
        .scalar_subquery()
    )

    stmt = (
        select(
            Post,
            User.manner_temp,
            like_count_sq.label("like_count"),
            chat_count_sq.label("chat_count"),
        )
        .join(User, User.email == Post.seller_email)
        .where(Post.id == post_id, Post.is_draft == False)  # noqa: E712
    )
    row = (await db.execute(stmt)).first()
    if row is None:
        return None
    post, manner_temp, like_count, chat_count = row
    return post, like_count, chat_count, manner_temp


async def increment_view_count(db: AsyncSession, post_id: int) -> None:
    stmt = (
        update(Post)
        .where(Post.id == post_id)
        .values(view_count=Post.view_count + 1)
    )
    await db.execute(stmt)
    await db.commit()
```

### carrot-api: service get_post_detail

```python
async def get_post_detail(db: AsyncSession, post_id: int) -> PostDetailResponse:
    result = await repository.get_post_detail(db, post_id)
    if result is None:
        raise AppException(
            code="POST_NOT_FOUND",
            detail="게시글을 찾을 수 없습니다.",
            status_code=404,
        )
    post, like_count, chat_count, manner_temp = result
    await repository.increment_view_count(db, post_id)
    return PostDetailResponse(
        id=post.id,
        seller_email=post.seller_email,
        title=post.title,
        description=post.description,
        price=post.price,
        is_free=post.is_free,
        status=post.status,
        trade_place=post.trade_place,
        photos=post.photos,
        view_count=post.view_count + 1,  # 증가 후 반환
        created_at=post.created_at,
        updated_at=post.updated_at,
        chat_count=chat_count,
        like_count=like_count,
        manner_temp=manner_temp,
    )
```

> view_count는 `increment_view_count` 실행 후 `post.view_count + 1`로 반환 (DB refresh 없이)

### carrot-api: router /{post_id} 위치 주의

```python
# router.py: POST / 다음에 추가 (literal path /draft, /upload-url 이후)
@router.get("/{post_id}", response_model=PostDetailResponse)
async def get_post(
    post_id: int,
    db: AsyncSession = Depends(get_db),
) -> PostDetailResponse:
    return await service.get_post_detail(db, post_id)
```

> `/{post_id}`는 라우터 등록 순서상 `/draft`, `/upload-url` 뒤에 위치해야 함 — 현재 파일 구조에서 맨 아래 추가하면 됨.

### carrot-web: ImageSlider 인라인 구현

```tsx
// posts/[id]/page.tsx 내 인라인 ImageSlider
function ImageSlider({ photos }: { photos: string[] }) {
  const [idx, setIdx] = useState(0)
  const startX = useRef(0)
  const dragging = useRef(false)

  const prev = () => setIdx((i) => Math.max(0, i - 1))
  const next = () => setIdx((i) => Math.min(photos.length - 1, i + 1))

  return (
    <div
      className="relative w-full h-60 overflow-hidden bg-gray-100 select-none"
      onMouseDown={(e) => { startX.current = e.clientX; dragging.current = true }}
      onMouseMove={(e) => {
        if (!dragging.current) return
        if (e.clientX - startX.current > 50) { prev(); dragging.current = false }
        if (startX.current - e.clientX > 50) { next(); dragging.current = false }
      }}
      onMouseUp={() => { dragging.current = false }}
      onMouseLeave={() => { dragging.current = false }}
    >
      <div
        className="flex h-full transition-transform duration-300 ease-out"
        style={{ transform: `translateX(-${idx * 100}%)`, width: `${photos.length * 100}%` }}
      >
        {photos.map((url, i) => (
          <div key={i} className="relative flex-shrink-0 h-full" style={{ width: `${100 / photos.length}%` }}>
            <Image src={url} alt={`사진 ${i + 1}`} fill className="object-cover" priority={i === 0} />
          </div>
        ))}
      </div>
      {photos.length > 1 && (
        <div className="absolute bottom-2 left-1/2 -translate-x-1/2 flex gap-1.5">
          {photos.map((_, i) => (
            <div key={i} className={`w-1.5 h-1.5 rounded-full ${i === idx ? 'bg-white' : 'bg-white/50'}`} />
          ))}
        </div>
      )}
    </div>
  )
}
```

### carrot-web: 소유자 판별 패턴

```tsx
// useQuery로 현재 사용자 정보 가져오기 (401 → null)
const { data: me } = useQuery({
  queryKey: ['me'],
  queryFn: () => usersApi.getMe().catch(() => null),
  retry: false,
})

const isOwner = me?.email === post?.seller_email
```

### carrot-web: 하단 고정 버튼 영역

```tsx
{/* 페이지 전체에 pb-20 추가 (하단 버튼 overlap 방지) */}
<div className="fixed bottom-0 left-0 right-0 max-w-lg mx-auto bg-white border-t px-4 py-3 flex gap-2">
  {isOwner ? (
    <>
      <Button variant="outline" className="flex-1" onClick={handleEdit}>수정</Button>
      <Button variant="outline" className="flex-1 text-red-500 border-red-300" onClick={handleDelete}>삭제</Button>
    </>
  ) : (
    <Button className="flex-1" style={{ backgroundColor: '#FF7E36' }}>채팅하기</Button>
  )}
</div>
```

> "수정" → `/posts/${id}/edit` (Story 2.5에서 구현), "삭제" → Story 2.5에서 구현. 이 스토리에서는 버튼 표시만 구현.
> "채팅하기" → Story 3.1에서 구현. 이 스토리에서는 버튼 표시만.

### carrot-mobile: Flutter PageView 슬라이더 패턴

```dart
// lib/features/posts/presentation/post_detail_screen.dart

class PostDetailScreen extends ConsumerStatefulWidget {
  final int postId;
  const PostDetailScreen({super.key, required this.postId});
  // ...
}

// ImageSlider with PageView:
final _pageCtrl = PageController();
int _imageIdx = 0;

PageView.builder(
  controller: _pageCtrl,
  onPageChanged: (i) => setState(() => _imageIdx = i),
  itemCount: photos.length,
  itemBuilder: (ctx, i) => Image.network(photos[i], fit: BoxFit.cover),
)

// 인디케이터:
if (photos.length > 1)
  Row(
    mainAxisAlignment: MainAxisAlignment.center,
    children: List.generate(photos.length, (i) => AnimatedContainer(
      duration: const Duration(milliseconds: 200),
      margin: const EdgeInsets.symmetric(horizontal: 2),
      width: i == _imageIdx ? 8 : 6,
      height: 6,
      decoration: BoxDecoration(
        color: i == _imageIdx ? Colors.white : Colors.white54,
        borderRadius: BorderRadius.circular(3),
      ),
    )),
  )
```

### carrot-mobile: 소유자 판별 패턴

```dart
// PostDetailScreen: try getMe() → compare email
Future<void> _checkOwner() async {
  try {
    final client = ref.read(apiClientProvider);
    // Users API 호출
    final res = await client.get(ApiEndpoints.usersMe);
    final myEmail = (res as Map<String, dynamic>)['email'] as String?;
    if (mounted) setState(() => _isOwner = myEmail == _post?['seller_email']);
  } catch (_) {
    // 비로그인 → _isOwner = false
  }
}
```

### carrot-mobile: MannerTempWidget 재사용

`lib/shared/widgets/manner_temp_widget.dart` 이미 존재. inline variant로 사용:
```dart
// 크기 조정이 필요하면 fontSize 파라미터 추가 고려
// 현재 구현: 18px, Bold — 상세 화면에는 14~16px이 적합
// → 별도 스타일 적용하거나 fontSize 옵션 파라미터 추가
```

> MannerTempWidget 현재 fontSize=18 고정. 상세 화면 판매자 프로필 영역에서 inline 사용 시 스타일 조정.
> 방법: `MannerTempWidget`에 `fontSize` optional 파라미터 추가 (기본값 18 유지) — 기존 사용처 영향 없음.

### 기존 코드 보존 주의사항

- `create_post` service: 기존 `PostDetailResponse.model_validate(post)` → 명시적 생성으로 변경 필요 (Task 1)
- `get_posts` repository의 `is_draft == False` 필터: 유지
- `get_draft` / `upsert_draft` repository: 변경 없음
- Flutter `FeedScreen`: `_loadPosts()` 로직 변경 없음, `PostCard` onTap만 추가

### Story 2.3 학습 사항 적용

- Flutter `PopScope` 대신 `onPopInvokedWithResult` 사용 패턴 확인됨 — 새 화면엔 불필요
- `mounted` 가드는 모든 async 완료 후 setState 전에 체크
- `ApiException(statusCode: 404)` catch 패턴: `on ApiException catch (e) { if (e.statusCode == 404) ... }`
- DB commit 후 IntegrityError → rollback + AppException
- 테스트: `.venv/Scripts/python.exe -m pytest` 사용

### References

- [Source: epics.md#Story 2.4] — AC 전문, FR-8
- [Source: ux-design-specification.md#ImageSlider] — 레이아웃 명세 (240px, 4:3, 인디케이터)
- [Source: ux-design-specification.md#MannerTempWidget] — inline variant 색상 범위
- [Source: ux-design-specification.md#게시글 상세] — 하단 고정 CTA 버튼 패턴

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- `GET /{post_id}` 라우터 등록 위치: POST `/draft`, `/upload-url` 뒤, POST `/` 앞에 위치시킴으로써 literal path 우선 매칭 보장

### Completion Notes List

- **Task 1**: PostDetailResponse에 chat_count(=0), like_count(=0), manner_temp(=36.5) 기본값 추가. create_post service를 명시적 생성으로 전환하여 하위호환 유지.
- **Task 2**: User JOIN + 두 서브쿼리(like_count, chat_count) 단일 SELECT. increment_view_count는 원자적 UPDATE.
- **Task 3**: get_post_detail service: 없으면 POST_NOT_FOUND(404), view_count+1 포함 반환.
- **Task 4**: GET /{post_id} 인증 불필요, Path(ge=1) 검증.
- **Task 5**: test_get_post_detail_success, test_get_post_detail_not_found 추가. 전체 26개 통과.
- **Task 6**: PostDetailResponse에 chat_count, like_count, manner_temp 추가. postsApi.getPost() 추가.
- **Task 7**: ImageSlider 인라인 구현 (마우스 드래그 + 점 인디케이터). usersApi.getMe() → 소유자 판별 (401 무음처리). 수정/삭제/채팅 버튼 Story 2.5/3.1 scope 표시.
- **Task 8**: PostsRepository.getPost(id) 추가.
- **Task 9**: PostDetailScreen: CustomScrollView + SliverAppBar + PageView 슬라이더. _checkOwner()로 비동기 소유자 판별.
- **Task 10**: FeedScreen PostCard onTap → PostDetailScreen(postId) 연결.

### File List

- `carrot-api/app/features/posts/schemas.py` (수정: PostDetailResponse chat_count/like_count/manner_temp 추가)
- `carrot-api/app/features/posts/repository.py` (수정: get_post_detail, increment_view_count, User import 추가)
- `carrot-api/app/features/posts/service.py` (수정: get_post_detail 추가, create_post 명시적 생성 전환)
- `carrot-api/app/features/posts/router.py` (수정: GET /{post_id} 추가, Path import 추가)
- `carrot-api/tests/unit/features/test_posts_service.py` (수정: get_post_detail 테스트 2개 추가)
- `carrot-web/src/lib/api/posts.ts` (수정: PostDetailResponse 확장, getPost() 추가)
- `carrot-web/src/app/posts/[id]/page.tsx` (신규: 게시글 상세 페이지)
- `carrot-mobile/lib/features/posts/data/posts_repository.dart` (수정: getPost() 추가)
- `carrot-mobile/lib/features/posts/presentation/post_detail_screen.dart` (신규: 상세 화면)
- `carrot-mobile/lib/features/posts/presentation/feed_screen.dart` (수정: PostCard onTap 연결)

## Change Log

- **2026-05-30**: Story 2.4 구현 완료 — GET /{post_id} API, view_count 증가, Next.js 상세 페이지 (ImageSlider), Flutter PostDetailScreen, FeedScreen 탭 연결. 26개 단위 테스트 통과.
- **2026-05-30**: 코드 리뷰 완료 — 0 decision-needed, 5 patch, 9 defer, 3 dismissed.

### Review Findings

#### Patch (모두 적용 완료)
- [x] [Review][Patch] P1: [High/AC#4] ImageSlider touch 이벤트 없음 — 모바일 브라우저 스와이프 불가 [page.tsx:ImageSlider]
- [x] [Review][Patch] P2: [High/AC#6] Flutter 버튼들 no-op — Edit/Delete/채팅 모두 빈 onPressed [post_detail_screen.dart]
- [x] [Review][Patch] P3: [Med] staleTime:60_000으로 logout 후 60초간 소유자 버튼 노출 [page.tsx:me query]
- [x] [Review][Patch] P4: [Low] ImageSlider onMouseLeave startX 미초기화 [page.tsx:ImageSlider]
- [x] [Review][Patch] P5: [Low] FeedScreen postId=0 fallback → 422 에러 [feed_screen.dart]

#### Deferred
- [x] [Review][Defer] chat_count가 messages 수 (chat rooms 아님) — deferred, 기존 2.1 패턴과 동일
- [x] [Review][Defer] seller_email PII 노출 — deferred, 포트폴리오 scope
- [x] [Review][Defer] is_owner 서버사이드 필드 없음 — deferred, 포트폴리오 scope
- [x] [Review][Defer] view_count 반환값 concurrency 시 stale — deferred, 포트폴리오 허용 범위
- [x] [Review][Defer] manner_temp 기본값 36.5가 JOIN 실패 은닉 — deferred, portfolio
- [x] [Review][Defer] 삭제된 seller → 미기록 404 — deferred, 아키텍처 이슈
- [x] [Review][Defer] Flutter getPost/getMe 두 번 API 호출 — deferred, 포트폴리오 scope
- [x] [Review][Defer] view_count 익명 farming 가능 — deferred, 포트폴리오 scope
- [x] [Review][Defer] NaN postId → loading 없이 404 UI — deferred, 허용 가능한 UX
