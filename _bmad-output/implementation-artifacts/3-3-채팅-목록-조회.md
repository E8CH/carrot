# Story 3.3: 채팅 목록 조회

Status: done

## Story

As a 로그인한 회원,
I want to see a list of all my chat rooms with key context,
So that I can quickly find and resume any conversation.

## Acceptance Criteria

1. 로그인한 회원이 `GET /api/v1/chats`를 호출하면 내가 참여한 채팅방 목록이 최근 메시지 기준 내림차순으로 반환된다 (JWT 필요)
2. 각 항목에 `room_id, post_id, post_thumbnail, opponent_email, seller_email, buyer_email, last_message, last_sent_at, unread_count`가 포함된다
3. Next.js `/chat` 페이지에서 ChatListTile 목록이 렌더링된다 (게시글 썸네일·상대방 이메일·마지막 메시지·경과시간)
4. Flutter 채팅 탭(index=1)에서 `_PlaceholderScreen` 대신 실제 `ChatListScreen`이 표시된다
5. 경과 시간은 클라이언트에서 계산한다 (방금 전/N분 전/N시간 전/N일 전)
6. 채팅 목록 항목 탭 시 해당 채팅방으로 이동한다 (`/chat/[roomId]`)

## Tasks / Subtasks

- [x] Task 1: carrot-api — schemas.py 수정 (AC: #1, #2)
  - `ChatRoomListItem`: `{room_id, post_id, post_thumbnail: str|None, opponent_email, seller_email, buyer_email, last_message, last_sent_at: datetime, unread_count: int}`

- [x] Task 2: carrot-api — repository.py 수정 (AC: #1, #2)
  - `get_chat_rooms(db, user_email) -> list[tuple]`
  - latest_id 서브쿼리, unread_count 서브쿼리, Post JOIN, ORDER BY sent_at DESC

- [x] Task 3: carrot-api — service.py 수정 (AC: #1, #2)
  - `list_chat_rooms(db, user_email) -> list[ChatRoomListItem]`

- [x] Task 4: carrot-api — router.py 수정 (AC: #1, #2)
  - `GET /` → `list_chat_rooms` (JWT 필요)

- [x] Task 5: carrot-api — 단위 테스트 2개 추가 (41/41 통과)

- [x] Task 6: carrot-web — chats.ts 수정
  - `ChatRoomListItem` 인터페이스, `getChatRooms()` 추가

- [x] Task 7: carrot-web — `src/app/chat/page.tsx` 신규 (AC: #3, #5, #6)
  - ChatListTile, formatElapsed, 채팅방 이동 (query params)

- [x] Task 8: carrot-mobile — `chats_repository.dart` 수정
  - `getChatRooms() -> List<Map<String, dynamic>>`

- [x] Task 9: carrot-mobile — `chat_list_screen.dart` 신규 (AC: #4, #5, #6)
  - ConsumerStatefulWidget, _fetchMyEmail, RefreshIndicator, ChatListTile, _formatElapsed

- [x] Task 10: carrot-mobile — `main_scaffold.dart` 수정 (AC: #4)
  - static const → final, ChatListScreen() 교체

## Dev Notes

### carrot-api: schemas.py 추가

```python
class ChatRoomListItem(BaseModel):
    room_id: str
    post_id: int
    post_thumbnail: str | None
    opponent_email: str
    seller_email: str
    buyer_email: str
    last_message: str
    last_sent_at: datetime
    unread_count: int
```

### carrot-api: repository.py — get_chat_rooms 쿼리 설계

```python
from sqlalchemy import func, or_
from sqlalchemy.orm import aliased
from app.features.posts.models import Post

async def get_chat_rooms(db: AsyncSession, user_email: str) -> list:
    # 서브쿼리 1: 사용자 참여 room의 가장 최신 메시지 ID
    latest_id_subq = (
        select(
            ChatMessage.room_id,
            func.max(ChatMessage.id).label("max_id"),
        )
        .where(
            or_(
                ChatMessage.seller_email == user_email,
                ChatMessage.buyer_email == user_email,
            )
        )
        .group_by(ChatMessage.room_id)
        .subquery("latest_id")
    )

    # 서브쿼리 2: room별 미읽음 메시지 수
    unread_subq = (
        select(
            ChatMessage.room_id,
            func.count(ChatMessage.id).label("unread_count"),
        )
        .where(
            or_(
                ChatMessage.seller_email == user_email,
                ChatMessage.buyer_email == user_email,
            ),
            ChatMessage.is_read == False,  # noqa: E712
            ChatMessage.sender_email != user_email,
        )
        .group_by(ChatMessage.room_id)
        .subquery("unread")
    )

    # 본 쿼리: 최신 메시지 + Post.photos + unread_count
    stmt = (
        select(
            ChatMessage,
            Post.photos,
            unread_subq.c.unread_count,
        )
        .join(latest_id_subq, ChatMessage.id == latest_id_subq.c.max_id)
        .join(Post, Post.id == ChatMessage.post_id)
        .outerjoin(unread_subq, unread_subq.c.room_id == ChatMessage.room_id)
        .order_by(ChatMessage.sent_at.desc())
    )

    return list((await db.execute(stmt)).all())
```

**Post import 추가**: `from app.features.posts.models import Post`

### carrot-api: service.py — list_chat_rooms

```python
async def list_chat_rooms(db: AsyncSession, user_email: str) -> list[ChatRoomListItem]:
    rows = await repository.get_chat_rooms(db, user_email)
    result = []
    for msg, photos, unread_count in rows:
        opponent_email = msg.buyer_email if user_email == msg.seller_email else msg.seller_email
        result.append(ChatRoomListItem(
            room_id=msg.room_id,
            post_id=msg.post_id,
            post_thumbnail=photos[0] if photos else None,
            opponent_email=opponent_email,
            seller_email=msg.seller_email,
            buyer_email=msg.buyer_email,
            last_message=msg.message,
            last_sent_at=msg.sent_at,
            unread_count=unread_count or 0,
        ))
    return result
```

**schemas import 추가**: `ChatRoomListItem`

### carrot-api: router.py — GET /

```python
@router.get("/", response_model=list[ChatRoomListItem])
async def list_chats(
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> list[ChatRoomListItem]:
    user_email = current_user.get("sub")
    if not user_email:
        raise AppException(code="UNAUTHORIZED", detail="유효하지 않은 토큰입니다.", status_code=401)
    return await service.list_chat_rooms(db, user_email)
```

**Import 추가**: `ChatRoomListItem`

### 단위 테스트 패턴

```python
@pytest.mark.asyncio
async def test_list_chat_rooms_success():
    """채팅 목록 정상 반환"""
    db = AsyncMock()
    msg = make_mock_message()
    photos = ["https://example.com/thumb.jpg"]
    unread = 2
    rows = [(msg, photos, unread)]

    with patch("app.features.chats.service.repository.get_chat_rooms", new=AsyncMock(return_value=rows)):
        result = await list_chat_rooms(db, "seller@example.com")

    assert len(result) == 1
    assert result[0].opponent_email == "buyer@example.com"
    assert result[0].unread_count == 2
    assert result[0].post_thumbnail == "https://example.com/thumb.jpg"


@pytest.mark.asyncio
async def test_list_chat_rooms_empty():
    """채팅방 없을 때 빈 리스트 반환"""
    db = AsyncMock()
    with patch("app.features.chats.service.repository.get_chat_rooms", new=AsyncMock(return_value=[])):
        result = await list_chat_rooms(db, "user@example.com")
    assert result == []
```

### carrot-web: chats.ts 추가

```typescript
export interface ChatRoomListItem {
  room_id: string
  post_id: number
  post_thumbnail: string | null
  opponent_email: string
  seller_email: string
  buyer_email: string
  last_message: string
  last_sent_at: string
  unread_count: number
}

// chatsApi에 추가
getChatRooms: (): Promise<ChatRoomListItem[]> =>
  apiClient.get<ChatRoomListItem[]>('/api/v1/chats/').then((r) => r.data),
```

### carrot-web: /chat/page.tsx (신규)

경과시간 계산 유틸:
```tsx
function formatElapsed(isoString: string): string {
  const diff = Date.now() - new Date(isoString).getTime()
  const minutes = Math.floor(diff / 60000)
  if (minutes < 1) return '방금 전'
  if (minutes < 60) return `${minutes}분 전`
  const hours = Math.floor(minutes / 60)
  if (hours < 24) return `${hours}시간 전`
  return `${Math.floor(hours / 24)}일 전`
}
```

채팅방 이동 시 query params 포함:
```tsx
router.push(
  `/chat/${room.room_id}?postId=${room.post_id}&sellerEmail=${encodeURIComponent(room.seller_email)}&buyerEmail=${encodeURIComponent(room.buyer_email)}`
)
```

현재 사용자 이메일은 `usersApi.getMe()`로 조회.

### carrot-mobile: chats_repository.dart 추가

```dart
Future<List<Map<String, dynamic>>> getChatRooms() async {
  final result = await _client.get(ApiEndpoints.chats);
  return (result as List).cast<Map<String, dynamic>>();
}
```

### carrot-mobile: chat_list_screen.dart (신규)

```dart
class ChatListScreen extends ConsumerStatefulWidget { ... }

// 경과시간 계산
String _formatElapsed(String isoString) {
  final diff = DateTime.now().difference(DateTime.parse(isoString));
  if (diff.inMinutes < 1) return '방금 전';
  if (diff.inMinutes < 60) return '${diff.inMinutes}분 전';
  if (diff.inHours < 24) return '${diff.inHours}시간 전';
  return '${diff.inDays}일 전';
}
```

ChatListTile 탭 시 ChatRoomScreen으로 이동:
```dart
Navigator.of(context).push(MaterialPageRoute(
  builder: (_) => ChatRoomScreen(
    roomId: room['room_id'] as String,
    sellerEmail: room['seller_email'] as String,
    buyerEmail: room['buyer_email'] as String,
    myEmail: _myEmail ?? '',
    postId: room['post_id'] as int,
  ),
));
```

`_myEmail` 조회: `initState`에서 `GET /api/v1/users/me` 호출.

### carrot-mobile: main_scaffold.dart 수정

```dart
// 기존
_PlaceholderScreen(label: '채팅 목록'),  // Story 3.3에서 교체

// 변경
ChatListScreen(),  // Story 3.3 구현
```

**import 추가**: `import '../../features/chats/presentation/chat_list_screen.dart';`

**주의**: `_pages`는 `static const`이므로 `ChatListScreen()`은 `const` 생성자여야 함. `ConsumerStatefulWidget`은 `const` 불가 → `_pages`를 `const`에서 `final`로 변경.

### 기존 코드 보존 필수

- `chat/[roomId]/page.tsx` 변경 없음 (Story 3.2 구현체 보존)
- `main_scaffold.dart` FAB, NavigationBar 로직 보존
- `ChatRoomScreen` 생성자 변경 없음 (Story 3.2 구현체)
- chats router 기존 엔드포인트 (`POST /`, `POST/GET /{room_id}/messages`) 보존

### Story 3.2 학습 사항 적용

- `or_(...)` SQLAlchemy 조건 패턴 (seller OR buyer)
- `aliased` 불필요 — subquery로 해결
- Cross-domain import: `from app.features.posts.models import Post` (posts.models에서 Post 직접 import)
- `noqa: E712` 주석 — SQLAlchemy `== False` 비교
- 테스트: `.venv/Scripts/python.exe -m pytest tests/unit/ -q`
- Flutter `if (!mounted) return` async 후 체크
- `_myEmail` 패턴: `initState` + `/users/me` API 호출로 현재 사용자 이메일 확인

### References

- [Source: epics.md#Story 3.3] — AC 전문
- [Source: epics.md#FR-14] — 게시글 썸네일·상대방·마지막 메시지·경과시간·최근 메시지 기준 내림차순
- [Source: architecture.md#TanStack Query] — queryKey: `['chats']`
- [Source: main_scaffold.dart] — `_PlaceholderScreen` 교체 위치 및 `_pages` static const 제약

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

(구현 중 채워질 예정)

### Completion Notes List

- **Task 1**: ChatRoomListItem(room_id, post_id, post_thumbnail, opponent_email, seller_email, buyer_email, last_message, last_sent_at, unread_count)
- **Task 2**: get_chat_rooms — latest_id 서브쿼리(max id per room) + unread_subq(is_read=False, sender!=user) + Post JOIN + outerjoin unread + ORDER BY sent_at DESC
- **Task 3**: list_chat_rooms — rows 순회하여 ChatRoomListItem 빌드, opponent_email 유추(seller vs buyer)
- **Task 4**: GET / → list_chat_rooms (JWT 필요, list[ChatRoomListItem] 반환)
- **Task 5**: 2개 테스트 (성공+unread, 빈 목록). 41/41 통과
- **Task 6**: ChatRoomListItem 인터페이스, getChatRooms() → GET /api/v1/chats/
- **Task 7**: ChatListTile(썸네일+이메일+메시지+경과시간+미읽음 배지), formatElapsed, handleRoomClick(query params)
- **Task 8**: getChatRooms() → GET ${chats}, List cast
- **Task 9**: ConsumerStatefulWidget, _init(email+rooms), _formatElapsed, RefreshIndicator, _buildTile, _openRoom→ChatRoomScreen
- **Task 10**: static const → final, ChatListScreen() 교체, import 추가

### File List

- `carrot-api/app/features/chats/schemas.py` (수정: ChatRoomListItem 추가)
- `carrot-api/app/features/chats/repository.py` (수정: get_chat_rooms 추가, Post import)
- `carrot-api/app/features/chats/service.py` (수정: list_chat_rooms 추가)
- `carrot-api/app/features/chats/router.py` (수정: GET / 추가)
- `carrot-api/tests/unit/features/test_chats_service.py` (수정: 2개 테스트 추가)
- `carrot-web/src/lib/api/chats.ts` (수정: ChatRoomListItem, getChatRooms 추가)
- `carrot-web/src/app/chat/page.tsx` (신규: 채팅 목록 페이지)
- `carrot-mobile/lib/features/chats/data/chats_repository.dart` (수정: getChatRooms 추가)
- `carrot-mobile/lib/features/chats/presentation/chat_list_screen.dart` (신규)
- `carrot-mobile/lib/shared/widgets/main_scaffold.dart` (수정: ChatListScreen 교체)

### Review Findings

- [x] [Review][Defer] Web Image 컴포넌트 외부 URL 도메인 설정 필요 (next.config.ts) — posts에서 동일 패턴, pre-existing issue — deferred
- [x] [Review][Defer] Flutter _fetchMyEmail 실패 시 myEmail='' → ChatRoomScreen 내 메시지 구분 불가 — 포트폴리오 scope — deferred
- ✅ Clean review — 치명적 버그 없음, SQL 쿼리 로직 정확, service 로직 올바름

## Change Log

- **2026-05-30**: Story 3.3 생성 — 채팅 목록 API, ChatListTile, 경과시간, 채팅 탭 실제 구현
- **2026-05-30**: Story 3.3 구현 완료 — GET /chats 목록 API, ChatListTile Web+Flutter, 경과시간, 미읽음 배지, Flutter 채팅 탭 실제 화면 연결. 41/41 통과
