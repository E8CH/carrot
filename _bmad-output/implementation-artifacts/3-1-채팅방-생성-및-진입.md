# Story 3.1: 채팅방 생성 및 진입

Status: done

## Story

As a 로그인한 구매자,
I want to start a chat with a seller by tapping "채팅하기" on a post,
So that I can negotiate and arrange a trade.

## Acceptance Criteria

1. 로그인한 구매자가 `POST /api/v1/chats`를 호출하면 (`{post_id}` 포함) 동일 (post_id, buyer_email) 조합의 채팅방이 없으면 새로 생성되고 room 정보가 반환된다 (JWT 필요)
2. 이미 존재하면 기존 room 정보가 반환된다 (중복 생성 방지)
3. 판매자 본인의 게시글에서 채팅하기를 시도하면 `{"detail": "본인 게시글에는 채팅할 수 없습니다.", "code": "SELF_CHAT_FORBIDDEN"}` 403 에러가 반환된다
4. Next.js 게시글 상세에서 "채팅하기" 버튼을 클릭하면 채팅방이 생성(또는 기존 방 반환)되고 채팅방 페이지(`/chat/[roomId]`)로 이동한다
5. Flutter 게시글 상세에서 하단 고정 "채팅하기" 버튼을 탭하면 동일한 흐름으로 채팅방 화면으로 이동한다

## Tasks / Subtasks

- [x] Task 1: carrot-api — schemas.py 신규 (AC: #1, #2, #3)
  - `CreateChatRequest`: `{post_id: int (ge=1)}`
  - `ChatRoomResponse`: `{room_id: str, post_id: int, seller_email: str, buyer_email: str}`

- [x] Task 2: carrot-api — repository.py 신규 (AC: #1, #2)
  - `get_or_create_room(db, post_id, buyer_email) -> str`: (post_id, buyer_email) 기반 기존 room_id 조회. 없으면 새 UUID 반환 (DB 저장 없음 — 첫 메시지 전송 시 Story 3.2에서 저장됨)

- [x] Task 3: carrot-api — service.py 신규 (AC: #1, #2, #3)
  - `create_or_get_chat_room(db, buyer_email, post_id) -> ChatRoomResponse`
  - 게시글 존재 확인, SELF_CHAT_FORBIDDEN 검증, room_id 조회/생성

- [x] Task 4: carrot-api — router.py 신규 (AC: #1, #2, #3)
  - `POST /` → `create_or_get_chat_room` (JWT 필요)

- [x] Task 5: carrot-api — app/api/v1/router.py 수정
  - chats router 등록 (`prefix="/chats"`)

- [x] Task 6: carrot-api — 단위 테스트 3개 신규 (36/36 통과)

- [x] Task 7: carrot-web — `src/lib/api/chats.ts` 신규
  - `chatsApi.createOrGetRoom(postId) -> ChatRoomResponse`

- [x] Task 8: carrot-web — `src/app/chat/[roomId]/page.tsx` 신규 (채팅방 shell) (AC: #4)
  - AppBar + 빈 메시지 영역 + "채팅 기능은 Story 3.2에서 구현됩니다" 안내

- [x] Task 9: carrot-web — `/posts/[id]/page.tsx` 수정 (AC: #4)
  - `alert(...)` 대신 실제 `chatsApi.createOrGetRoom` 호출 + `/chat/${roomId}` 이동

- [x] Task 10: carrot-mobile — `lib/features/chats/data/chats_repository.dart` 신규

- [x] Task 11: carrot-mobile — `lib/features/chats/presentation/chat_room_screen.dart` 신규 (shell) (AC: #5)

- [x] Task 12: carrot-mobile — `post_detail_screen.dart` 수정 (AC: #5)
  - `SnackBar` 플레이스홀더 → 실제 채팅방 생성 + `ChatRoomScreen` 이동

## Dev Notes

### DB 현황 (이미 완비)

`chat_messages` 테이블 — Story 1.1 마이그레이션에서 이미 생성됨:
```python
# app/features/chats/models.py (이미 존재)
class ChatMessage(Base):
    __tablename__ = "chat_messages"
    id: Mapped[int] = mapped_column(primary_key=True)
    room_id: Mapped[str] = mapped_column(UUID(as_uuid=False), nullable=False, index=True)
    seller_email: Mapped[str] = mapped_column(String(255), ForeignKey("users.email", ondelete="CASCADE"))
    buyer_email: Mapped[str] = mapped_column(String(255), ForeignKey("users.email", ondelete="CASCADE"))
    post_id: Mapped[int] = mapped_column(Integer, ForeignKey("posts.id", ondelete="CASCADE"))
    message: Mapped[str] = mapped_column(Text)
    is_read: Mapped[bool] = mapped_column(Boolean, default=False)
    sent_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
```

**`room_id` 설계**: 별도 `chat_rooms` 테이블 없음. `room_id`는 UUID 문자열.
- 채팅방 생성 = (post_id, buyer_email) 기준으로 기존 `room_id` 조회
- 없으면 서버에서 새 UUID 생성 후 반환 (DB에 저장하지 않음)
- 첫 메시지 전송(Story 3.2)에서 해당 room_id로 첫 chat_messages 레코드 생성
- 빈 방은 DB에 존재하지 않아도 됨 — 기존 방은 메시지로 식별

### carrot-api: schemas.py

```python
from pydantic import BaseModel, Field

class CreateChatRequest(BaseModel):
    post_id: int = Field(ge=1)

class ChatRoomResponse(BaseModel):
    room_id: str
    post_id: int
    seller_email: str
    buyer_email: str
```

### carrot-api: repository.py

```python
import uuid
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession
from app.features.chats.models import ChatMessage

async def get_or_create_room(db: AsyncSession, post_id: int, buyer_email: str) -> str:
    stmt = (
        select(ChatMessage.room_id)
        .where(ChatMessage.post_id == post_id, ChatMessage.buyer_email == buyer_email)
        .limit(1)
    )
    existing = (await db.execute(stmt)).scalar_one_or_none()
    if existing:
        return existing
    return str(uuid.uuid4())
```

### carrot-api: service.py

```python
from sqlalchemy.ext.asyncio import AsyncSession
from app.features.chats import repository
from app.features.chats.schemas import ChatRoomResponse
from app.features.posts import repository as posts_repository
from app.shared.exceptions import AppException

async def create_or_get_chat_room(
    db: AsyncSession,
    buyer_email: str,
    post_id: int,
) -> ChatRoomResponse:
    post = await posts_repository.get_post_by_id(db, post_id)
    if post is None:
        raise AppException(code="POST_NOT_FOUND", detail="게시글을 찾을 수 없습니다.", status_code=404)
    if post.seller_email == buyer_email:
        raise AppException(code="SELF_CHAT_FORBIDDEN", detail="본인 게시글에는 채팅할 수 없습니다.", status_code=403)
    room_id = await repository.get_or_create_room(db, post_id, buyer_email)
    return ChatRoomResponse(
        room_id=room_id,
        post_id=post_id,
        seller_email=post.seller_email,
        buyer_email=buyer_email,
    )
```

### carrot-api: router.py

```python
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_db
from app.core.dependencies import get_current_user
from app.features.chats import service
from app.features.chats.schemas import ChatRoomResponse, CreateChatRequest
from app.shared.exceptions import AppException

router = APIRouter()

@router.post("/", response_model=ChatRoomResponse, status_code=200)
async def create_or_get_chat(
    request: CreateChatRequest,
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> ChatRoomResponse:
    buyer_email = current_user.get("sub")
    if not buyer_email:
        raise AppException(code="UNAUTHORIZED", detail="유효하지 않은 토큰입니다.", status_code=401)
    return await service.create_or_get_chat_room(db, buyer_email, request.post_id)
```

**200 반환**: 생성·조회 모두 200. 201은 "반드시 새로 생성됨" 의미라 토글 패턴과 불일치.

### carrot-api: app/api/v1/router.py 수정

```python
from app.features.chats.router import router as chats_router
router.include_router(chats_router, prefix="/chats", tags=["chats"])
```

### carrot-api: 테스트 패턴 (conftest.py 자동 로드)

```python
# tests/unit/features/test_chats_service.py
# 테스트 실행: .venv/Scripts/python.exe -m pytest tests/unit/ -q
from unittest.mock import AsyncMock, MagicMock, patch
import pytest
from app.features.chats.schemas import ChatRoomResponse
from app.features.chats.service import create_or_get_chat_room
from app.shared.exceptions import AppException

def make_mock_post(seller_email="seller@example.com"):
    post = MagicMock()
    post.seller_email = seller_email
    return post

@pytest.mark.asyncio
async def test_create_chat_success():
    db = AsyncMock()
    post = make_mock_post()
    with (
        patch("app.features.chats.service.posts_repository.get_post_by_id", new=AsyncMock(return_value=post)),
        patch("app.features.chats.service.repository.get_or_create_room", new=AsyncMock(return_value="uuid-room-id")),
    ):
        result = await create_or_get_chat_room(db, "buyer@example.com", 1)
    assert isinstance(result, ChatRoomResponse)
    assert result.room_id == "uuid-room-id"

@pytest.mark.asyncio
async def test_self_chat_forbidden():
    db = AsyncMock()
    post = make_mock_post(seller_email="user@example.com")
    with patch("app.features.chats.service.posts_repository.get_post_by_id", new=AsyncMock(return_value=post)):
        with pytest.raises(AppException) as exc_info:
            await create_or_get_chat_room(db, "user@example.com", 1)
    assert exc_info.value.code == "SELF_CHAT_FORBIDDEN"
    assert exc_info.value.status_code == 403

@pytest.mark.asyncio
async def test_post_not_found():
    db = AsyncMock()
    with patch("app.features.chats.service.posts_repository.get_post_by_id", new=AsyncMock(return_value=None)):
        with pytest.raises(AppException) as exc_info:
            await create_or_get_chat_room(db, "buyer@example.com", 999)
    assert exc_info.value.code == "POST_NOT_FOUND"
    assert exc_info.value.status_code == 404
```

### carrot-web: chats.ts

```typescript
import apiClient from './axios'

export interface ChatRoomResponse {
  room_id: string
  post_id: number
  seller_email: string
  buyer_email: string
}

export const chatsApi = {
  createOrGetRoom: (postId: number): Promise<ChatRoomResponse> =>
    apiClient.post<ChatRoomResponse>('/api/v1/chats/', { post_id: postId }).then((r) => r.data),
}
```

### carrot-web: /chat/[roomId]/page.tsx (shell)

이 스토리에서는 shell만 구현. 메시지 전송/수신은 Story 3.2에서 추가.

```tsx
'use client'
import { useParams, useRouter } from 'next/navigation'

export default function ChatRoomPage() {
  const { roomId } = useParams<{ roomId: string }>()
  const router = useRouter()

  return (
    <div className="min-h-screen bg-white flex flex-col max-w-lg mx-auto">
      <header className="sticky top-0 z-10 bg-white border-b px-4 py-3 flex items-center gap-3">
        <button onClick={() => router.back()} className="text-gray-600 text-lg">←</button>
        <span className="font-semibold">채팅</span>
      </header>
      <div className="flex-1 flex items-center justify-center text-gray-300 text-sm">
        메시지 기능은 Story 3.2에서 구현됩니다.
      </div>
      <div className="border-t px-4 py-3 flex gap-2 bg-white">
        <input disabled placeholder="메시지 입력..." className="flex-1 border rounded-full px-4 py-2 text-sm bg-gray-50 cursor-not-allowed" />
        <button disabled className="px-4 py-2 rounded-full text-sm text-white bg-orange-300 cursor-not-allowed">전송</button>
      </div>
    </div>
  )
}
```

### carrot-web: /posts/[id]/page.tsx 수정

`alert(...)` 플레이스홀더를 실제 로직으로 교체:

```tsx
// 추가할 state
const [chatLoading, setChatLoading] = useState(false)

// handleChat 함수 추가
const handleChat = async () => {
  if (!me) { router.push('/auth/login'); return }
  setChatLoading(true)
  try {
    const room = await chatsApi.createOrGetRoom(postId)
    router.push(`/chat/${room.room_id}`)
  } catch {
    // 에러 시 아무것도 표시 안 함 (포트폴리오 scope)
  } finally {
    setChatLoading(false)
  }
}

// Button 변경
<Button ... onClick={handleChat} disabled={chatLoading}>
  {chatLoading ? '연결 중...' : '채팅하기'}
</Button>
```

**Import 추가**: `import { chatsApi } from '@/lib/api/chats'`

### carrot-mobile: chats_repository.dart

```dart
// lib/features/chats/data/chats_repository.dart
import '../../../core/api/api_client.dart';
import '../../../core/api/api_endpoints.dart';

class ChatsRepository {
  final ApiClient _client;
  const ChatsRepository(this._client);

  Future<Map<String, dynamic>> createOrGetRoom(int postId) async {
    final result = await _client.post(
      ApiEndpoints.chats,
      body: {'post_id': postId},
    );
    return result as Map<String, dynamic>;
  }
}
```

`ApiEndpoints.chats`는 이미 `'/api/v1/chats'`로 정의됨 (api_endpoints.dart 확인).

### carrot-mobile: ChatRoomScreen (shell)

```dart
// lib/features/chats/presentation/chat_room_screen.dart
class ChatRoomScreen extends ConsumerWidget {
  final String roomId;
  final String sellerEmail;
  final int postId;

  const ChatRoomScreen({
    super.key,
    required this.roomId,
    required this.sellerEmail,
    required this.postId,
  });

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(title: Text(sellerEmail)),
      body: const Center(
        child: Text('메시지 기능은 Story 3.2에서 구현됩니다.',
            style: TextStyle(color: Colors.grey)),
      ),
      bottomNavigationBar: Padding(
        padding: const EdgeInsets.fromLTRB(16, 8, 16, 24),
        child: Row(children: [
          Expanded(
            child: TextField(
              enabled: false,
              decoration: InputDecoration(
                hintText: '메시지 입력...',
                filled: true,
                fillColor: Colors.grey.shade100,
                border: OutlineInputBorder(
                  borderRadius: BorderRadius.circular(24),
                  borderSide: BorderSide.none,
                ),
                contentPadding: const EdgeInsets.symmetric(horizontal: 16, vertical: 10),
              ),
            ),
          ),
          const SizedBox(width: 8),
          ElevatedButton(
            onPressed: null,
            style: ElevatedButton.styleFrom(
              backgroundColor: AppColors.primary,
              shape: const CircleBorder(),
              padding: const EdgeInsets.all(12),
            ),
            child: const Icon(Icons.send, color: Colors.white, size: 20),
          ),
        ]),
      ),
    );
  }
}
```

### carrot-mobile: post_detail_screen.dart 수정

기존 SnackBar 플레이스홀더:
```dart
// 현재 (제거 대상)
ScaffoldMessenger.of(context).showSnackBar(
  const SnackBar(content: Text('채팅 기능은 Story 3.1에서 구현됩니다.')),
)
```

교체 대상 함수 (`_handleChat` 추가):
```dart
bool _chatLoading = false;

Future<void> _handleChat() async {
  if (_chatLoading) return;
  if (mounted) setState(() => _chatLoading = true);
  try {
    final client = ref.read(apiClientProvider);
    final repo = ChatsRepository(client);
    final result = await repo.createOrGetRoom(widget.postId);
    final roomId = result['room_id'] as String;
    final sellerEmail = result['seller_email'] as String;
    if (mounted) {
      Navigator.of(context).push(MaterialPageRoute(
        builder: (_) => ChatRoomScreen(
          roomId: roomId,
          sellerEmail: sellerEmail,
          postId: widget.postId,
        ),
      ));
    }
  } catch (_) {
    if (mounted) {
      ScaffoldMessenger.of(context).showSnackBar(
        const SnackBar(content: Text('채팅방 생성에 실패했습니다.')),
      );
    }
  } finally {
    if (mounted) setState(() => _chatLoading = false);
  }
}
```

하단 버튼:
```dart
ElevatedButton(
  onPressed: _chatLoading ? null : _handleChat,
  ...
  child: Text(_chatLoading ? '연결 중...' : '채팅하기', ...),
)
```

**Import 추가**:
```dart
import '../../chats/data/chats_repository.dart';
import '../../chats/presentation/chat_room_screen.dart';
```

### 기존 코드 보존 필수

- `post_detail_screen.dart`의 `_isOwner`, `_isLiked`, `_confirmDelete`, `_toggleLike` 로직 변경 없음
- 비오너 하단 바 구조: `[하트버튼] [채팅하기]` — 채팅하기 버튼의 `onPressed`만 변경
- `app/api/v1/router.py`: 기존 auth/users/posts router 등록 유지 + chats 추가
- `main_scaffold.dart`: 채팅 탭 placeholder는 Story 3.3에서 교체 예정, 이 스토리에서 건드리지 않음

### Story 2.6 학습 사항 적용

- `Path(ge=1)` 패턴 → `CreateChatRequest`의 `post_id: int = Field(ge=1)`
- 서비스 레이어에서 외부 도메인 repository 직접 import 패턴 (posts_repository 사용)
- Flutter `mounted` 체크 유지
- `ScaffoldMessenger` async 갭 전에 캡처 불필요 (여기서는 catch에서 직접 `if (mounted)` 체크로 충분)
- 테스트: `.venv/Scripts/python.exe -m pytest tests/unit/ -q`

### References

- [Source: epics.md#Story 3.1] — AC 전문
- [Source: epics.md#FR-12] — 동일 게시글+구매자 조합 중복 시 기존 방으로 이동
- [Source: architecture.md#채팅 폴링] — room_id 기반 조회, `GET /api/v1/chats/{room_id}/messages?after={last_message_id}`
- [Source: models.py#ChatMessage] — room_id UUID(as_uuid=False), 별도 rooms 테이블 없음

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

(구현 중 채워질 예정)

### Completion Notes List

- **Task 1**: CreateChatRequest(post_id: int ge=1), ChatRoomResponse(room_id, post_id, seller_email, buyer_email)
- **Task 2**: get_or_create_room → (post_id, buyer_email) 기존 room_id SELECT LIMIT 1. 없으면 uuid4() 반환. DB 저장 없음
- **Task 3**: create_or_get_chat_room → POST_NOT_FOUND(404), SELF_CHAT_FORBIDDEN(403), room_id 조회/생성
- **Task 4**: POST /chats/ (status_code=200, JWT 필요) → service.create_or_get_chat_room
- **Task 5**: app/api/v1/router.py에 chats_router prefix="/chats" 추가
- **Task 6**: 3개 테스트 (성공, 셀프챗, 404). 36/36 통과
- **Task 7**: chatsApi.createOrGetRoom(postId) → POST /api/v1/chats/ {post_id}
- **Task 8**: /chat/[roomId]/page.tsx shell — 헤더 + 빈 메시지 영역 + 비활성 입력창
- **Task 9**: handleChat 추가, chatLoading 상태, chatsApi.createOrGetRoom → router.push
- **Task 10**: ChatsRepository.createOrGetRoom(postId) → POST ApiEndpoints.chats
- **Task 11**: ChatRoomScreen(roomId, sellerEmail, postId) shell — AppBar + placeholder + 비활성 입력창
- **Task 12**: _chatLoading 상태, _handleChat → createOrGetRoom → ChatRoomScreen 이동. SnackBar 에러 처리

### File List

- `carrot-api/app/features/chats/schemas.py` (신규)
- `carrot-api/app/features/chats/repository.py` (신규)
- `carrot-api/app/features/chats/service.py` (신규)
- `carrot-api/app/features/chats/router.py` (신규)
- `carrot-api/app/api/v1/router.py` (수정: chats router 등록)
- `carrot-api/tests/unit/features/test_chats_service.py` (신규)
- `carrot-web/src/lib/api/chats.ts` (신규)
- `carrot-web/src/app/chat/[roomId]/page.tsx` (신규: shell)
- `carrot-web/src/app/posts/[id]/page.tsx` (수정: 채팅하기 실제 구현)
- `carrot-mobile/lib/features/chats/data/chats_repository.dart` (신규)
- `carrot-mobile/lib/features/chats/presentation/chat_room_screen.dart` (신규: shell)
- `carrot-mobile/lib/features/posts/presentation/post_detail_screen.dart` (수정: 채팅하기 실제 구현)

### Review Findings

- [x] [Review][Patch] Web `handleChat`: 에러 시 사용자 피드백 없음 → `chatError` 상태 추가, 하단 바에 에러 메시지 표시 [posts/[id]/page.tsx:handleChat] ✅ 패치 완료
- [x] [Review][Patch] Flutter `_handleChat`: async gap 후 `ScaffoldMessenger.of(context)` 사용 → messenger async 갭 전 캡처 [post_detail_screen.dart:_handleChat] ✅ 패치 완료
- [x] [Review][Defer] service.py cross-domain import (posts_repository) — 도메인 경계 위반이나 포트폴리오 scope 허용
- [x] [Review][Defer] `get_or_create_room`: ORDER BY 없음 — 동일 room_id 보장되므로 실질 영향 없음
- [x] [Review][Defer] Web handleChat: `me` 로딩 중 탭 시 로그인 리다이렉트 false positive — 드묾, 포트폴리오 scope

## Change Log

- **2026-05-30**: Story 3.1 생성 — 채팅방 생성/조회 API, Web+Flutter shell 및 채팅하기 버튼 연결
- **2026-05-30**: Story 3.1 구현 완료 — POST /chats/ 엔드포인트, chats 도메인 전체 신규, Web/Flutter 채팅하기 실제 연결. 36/36 단위 테스트 통과
