# Epic 6 회고 — 관리자 웹 (최종 에픽)

**날짜:** 2026-05-30
**프로젝트:** carrot
**참여:** HEMICOLON (Project Lead), Amelia (Developer), Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer), Elena (Junior Dev)

---

## 완료 지표

| 항목 | 수치 |
|------|------|
| 완료 스토리 | 5/5 (100%) |
| 단위 테스트 (Epic 6 시작) | 81개 |
| 단위 테스트 (Epic 6 완료) | 95개 |
| 코드 리뷰 패치 적용 | 9개 |
| 유예 처리 이슈 | 13개 |
| TypeScript 에러 | 0 |

---

## 성공 요인

1. **admin page 패턴 재사용**: Story 6.3에서 수립한 HTML table + TanStack Query + 토스트 패턴이 6.4까지 그대로 확장됨
2. **admin/repository.py 쿼리 분리**: admin 전용 쿼리를 별도 파일로 분리하는 구조가 6.3→6.4 확장을 자연스럽게 만들었음
3. **테스트 패턴 일관성**: `make_admin_dep()` + `AsyncMock` + `patch` 패턴이 6.2부터 확립되어 테스트 작성 속도가 향상됨
4. **코드 리뷰 효과**: Literal 타입 검증, UUID 패턴, 이메일 대소문자, .env 시크릿 Docker 노출 등 9개 실제 버그 발견 및 수정
5. **CS→DS→CR 자동 사이클**: 스토리 생성→구현→코드리뷰→패치까지 무중단 자동화

---

## 어려운 점

1. **ChatMessage GROUP BY 쿼리 (6.4)**: room_id 그룹핑 + Post JOIN + aggregate 함수 조합의 SQLAlchemy 표현이 섬세했음
2. **Supabase Storage mock 체인 (6.3)**: `_supabase.storage.from_(_BUCKET).remove()` 체인 mock 시 `mock.storage.from_` 정확한 체인 파악 필요
3. **배포 스토리 테스트 방법 (6.5)**: 인프라/설정 스토리의 단위 테스트 접근법 — CORS 파싱 로직으로 대체

---

## 전체 프로젝트 패턴

### 반복 성공 패턴
- `router → service → repository` 3계층 일관 적용
- JWT `Depends(get_current_user)` / `Depends(require_admin)` 보안 패턴
- TanStack Query queryKey 규칙 표준화

### 코드 리뷰 반복 발견 유형
- 타입 안전성 부족 (`Literal`, `UUID pattern`, 비교 연산)
- 환경 변수/시크릿 노출 위험
- 이메일 비교 대소문자 처리

---

## 다음 단계 (Railway 배포)

1. Railway 프로젝트 생성 + 3개 서비스 배포 (carrot-api, carrot-web, carrot-admin)
2. 환경변수 설정:
   - carrot-api: `DATABASE_URL` (postgresql+asyncpg://... 형식), `SUPABASE_URL`, `SUPABASE_KEY`, `JWT_SECRET`, `ALLOWED_ORIGINS`
   - carrot-web/admin: `NEXT_PUBLIC_API_URL` (빌드 시 설정)
3. CORS `ALLOWED_ORIGINS`에 실제 Railway 도메인 설정
4. Flutter 모바일 APK 빌드

---

## deferred-work.md 주요 항목

- DATABASE_URL `+asyncpg` 접두사 운영 문서화
- Docker 비루트 사용자 추가 (보안 강화)
- uv 버전 고정 (재현성)
- 채팅방/메시지 페이지네이션 (스케일업 시)
- 비존재 room_id 404 처리 (실서비스 전환 시)

---

## 프로젝트 최종 통계

| 에픽 | 스토리 수 | 완료 |
|------|----------|------|
| Epic 1: 플랫폼 초기화 & 회원 인증 | 4 | ✅ |
| Epic 2: 게시글 등록 & 피드 탐색 | 6 | ✅ |
| Epic 3: 1:1 채팅 | 4 | ✅ |
| Epic 4: 거래 완료 & 후기·매너온도 | 3 | ✅ |
| Epic 5: 나의 당근 (마이페이지) | 3 | ✅ |
| Epic 6: 관리자 웹 | 5 | ✅ |
| **합계** | **25** | **25/25 (100%)** |

**단위 테스트:** 95개 통과  
**플랫폼:** FastAPI + Next.js(×2) + Flutter  
**코드 리뷰 패치:** 에픽 전체 총 30+ 개 실제 이슈 수정
