# Story 1.1: 프로젝트 기반 환경 설정

Status: review

## Story

As a developer,
I want the monorepo and all 4 platform projects initialized with core infrastructure,
so that feature development can begin immediately with a consistent foundation.

## Acceptance Criteria

1. `carrot/` 모노레포 루트 하위에 `carrot-api/`, `carrot-web/`, `carrot-admin/`, `carrot-mobile/` 4개 프로젝트가 생성된다
2. `carrot-api/`는 FastAPI + SQLAlchemy 2.0 Async + Alembic이 설치되고 `GET /health`가 200을 반환한다
3. `carrot-web/`과 `carrot-admin/`은 Next.js 16 + TypeScript + Tailwind CSS + shadcn/ui가 초기화된다
4. `carrot-mobile/`은 Flutter 3탭 NavigationBar(홈/채팅/나의당근)가 구성된다
5. `carrot-web/`과 `carrot-admin/`의 `tailwind.config.ts`에 carrot 색상 토큰이 등록된다
6. `carrot-mobile/`의 `ThemeData`에 `ColorScheme.fromSeed(Color(0xFFFF7E36))`가 적용된다
7. `carrot-web/`과 `carrot-admin/`에 Axios 인스턴스(`src/lib/api/axios.ts`)와 TanStack Query `QueryClient`가 설정된다
8. `carrot-mobile/`에 Riverpod `ProviderScope`와 `ApiClient` 서비스 클래스가 설정된다
9. Alembic 초기 마이그레이션이 생성된다: `users`, `posts`, `chat_messages`, `likes`, `reviews` 테이블 + FK CASCADE + `idx_chat_messages_post_id_sent_at` 복합 인덱스
10. 각 플랫폼에 `.env.example` 파일이 생성된다

## Tasks / Subtasks

- [x] Task 1: 모노레포 루트 구조 생성 (AC: #1)
  - [x] 현재 작업 디렉토리(`C:\Users\HEMICOLON\Documents\WORK\VIBE\project\carrot`)가 모노레포 루트다. 이 아래에 4개 서브 디렉토리를 생성한다.
  - [x] `.gitignore` 파일 생성 (Python, Node, Flutter, .env 항목 포함)

- [x] Task 2: carrot-api — FastAPI 프로젝트 초기화 (AC: #2)
  - [x] uv venv 생성 및 의존성 설치: `fastapi uvicorn[standard] sqlalchemy[asyncio] aiosqlite alembic python-jose[cryptography] passlib[bcrypt] python-multipart httpx pydantic-settings supabase`
  - [x] `pyproject.toml` 작성 (Python 3.12+, 의존성 목록)
  - [x] `app/main.py` 생성 — FastAPI 앱, CORS 미들웨어, 헬스체크 엔드포인트, 라우터 등록, AppException 전역 핸들러
  - [x] `app/core/config.py` — Pydantic Settings (DATABASE_URL, SUPABASE_URL, SUPABASE_KEY, JWT_SECRET, JWT_ALGORITHM="HS256", ACCESS_TOKEN_EXPIRE_MINUTES=43200)
  - [x] `app/core/database.py` — AsyncEngine, AsyncSession, `get_db` Depends
  - [x] `app/core/security.py` — `create_access_token()`, `verify_token()` (python-jose)
  - [x] `app/core/dependencies.py` — `get_current_user`, `require_admin` Depends 함수
  - [x] `app/shared/exceptions.py` — `AppException` 클래스 + `app_exception_handler`
  - [x] `app/shared/schemas.py` — `PaginatedResponse[T]` 제네릭 (items + total + page + size)
  - [x] `app/api/v1/router.py` — 빈 라우터 (도메인별 라우터 include 위한 중앙 집결점)
  - [x] `.env.example` 작성

- [x] Task 3: carrot-api — SQLAlchemy 모델 & Alembic 마이그레이션 (AC: #9)
  - [x] `app/features/users/models.py` — `User` 모델 (id, email UNIQUE NOT NULL, hashed_password, address, phone, role ENUM('user','admin'), is_active, manner_temp FLOAT DEFAULT 36.5, created_at, updated_at)
  - [x] `app/features/posts/models.py` — `Post` 모델 (id, seller_email FK→users.email, title, description, price, is_free, status ENUM('판매중','예약중','거래완료'), trade_place, photos JSONB/TEXT[], buyer_email FK→users.email NULLABLE, view_count, is_draft, created_at, updated_at)
  - [x] `app/features/chats/models.py` — `ChatMessage` 모델 (id, room_id UUID, seller_email, buyer_email, post_id FK→posts.id CASCADE, message, is_read BOOL DEFAULT FALSE, sent_at)
  - [x] `app/features/posts/models.py`에 `Like` 모델 추가 (id, user_email FK→users.email, post_id FK→posts.id CASCADE, created_at, UNIQUE(user_email, post_id))
  - [x] `app/features/reviews/models.py` — `Review` 모델 (id, post_id FK→posts.id CASCADE, writer_email, target_email, is_positive BOOL, tags TEXT[], comment, created_at, UNIQUE(post_id, writer_email))
  - [x] `alembic/env.py` — `target_metadata = Base.metadata`, async 지원 설정
  - [x] `alembic revision --autogenerate` 실행하여 초기 마이그레이션 파일 생성
  - [x] 마이그레이션 파일에 `idx_chat_messages_post_id_sent_at` 인덱스 추가 확인 (없으면 수동 추가: `op.create_index('idx_chat_messages_post_id_sent_at', 'chat_messages', ['post_id', 'sent_at'])`)

- [x] Task 4: carrot-web — Next.js 초기화 (AC: #3, #5, #7)
  - [x] `npx create-next-app@latest carrot-web --typescript --eslint --app --src-dir --tailwind` 실행
  - [x] `npx shadcn@latest init` 실행 (style: Default, base color: Neutral, CSS variables: yes)
  - [x] `tailwind.config.ts` — carrot 색상 토큰 등록 (Tailwind v4 → globals.css CSS 변수로 등록)
  - [x] `src/lib/api/axios.ts` — Axios 인스턴스 생성 (baseURL: `process.env.NEXT_PUBLIC_API_URL`, 요청 인터셉터: localStorage에서 JWT 주입, 응답 인터셉터: 401 → `/auth/login` 리다이렉트)
  - [x] `src/lib/providers.tsx` — `QueryClientProvider` 래퍼 컴포넌트 (TanStack Query)
  - [x] `src/app/layout.tsx` — `providers.tsx`로 래핑
  - [x] `package.json`에 추가 패키지: `axios`, `@tanstack/react-query`, `@tanstack/react-query-devtools`
  - [x] `.env.example`: `NEXT_PUBLIC_API_URL=http://localhost:8000`

- [x] Task 5: carrot-admin — Next.js 초기화 (AC: #3, #5, #7)
  - [x] `npx create-next-app@latest carrot-admin --typescript --eslint --app --src-dir --tailwind` 실행
  - [x] `npx shadcn@latest init` 실행
  - [x] `tailwind.config.ts` — carrot 색상 토큰 등록 (Tailwind v4 → globals.css CSS 변수로 등록)
  - [x] `src/lib/api/axios.ts` — 동일 패턴 (관리자용 JWT 사용, 401 → `/auth/login` 리다이렉트)
  - [x] `src/lib/providers.tsx` + `src/app/layout.tsx` 설정 (carrot-web과 동일)
  - [x] `.env.example`: `NEXT_PUBLIC_API_URL=http://localhost:8000`

- [x] Task 6: carrot-mobile — Flutter 초기화 (AC: #4, #6, #8)
  - [x] `flutter create --org com.carrot --platforms android,ios --template app carrot_mobile` 실행 (디렉토리명 carrot-mobile로 변경)
  - [x] `pubspec.yaml` — 의존성 추가: `flutter_riverpod`, `riverpod_annotation`, `http`, `go_router`, `shared_preferences`, `flutter_lints`
  - [x] `lib/core/theme/app_colors.dart` — 색상 상수 정의 (primaryOrange, 매너온도 색상 등)
  - [x] `lib/core/theme/app_theme.dart` — `ThemeData.light(useMaterial3: true)` + `ColorScheme.fromSeed(seedColor: Color(0xFFFF7E36))`
  - [x] `lib/core/api/api_client.dart` — `http` 패키지 래퍼, `Authorization: Bearer $token` 헤더 자동 주입, 기본 에러 처리
  - [x] `lib/core/api/api_endpoints.dart` — API URL 상수 (`baseUrl`, 각 엔드포인트 경로)
  - [x] `lib/main.dart` — `ProviderScope` 최상위 래핑, `MaterialApp` with `theme`, `home: MainScaffold`
  - [x] `lib/shared/widgets/main_scaffold.dart` — `NavigationBar` 3탭 (홈/채팅/나의당근), 각 탭 Placeholder 화면
  - [x] `.env.example` 또는 `lib/core/api/api_endpoints.dart`에 base URL 환경 분리 주석 추가

## Dev Notes

### 핵심 제약 및 아키텍처 규칙

**이 스토리는 이후 모든 Epic의 기반이다. 잘못 설정하면 모든 후속 스토리가 영향받는다.**

1. **API 경계:** `GET /health`와 `POST /api/v1/auth/login`, `POST /api/v1/auth/register`만 공개. 나머지는 JWT 필수.
2. **에러 응답 형식 고정:** 항상 `{"detail": "...", "code": "..."}` — 이 형식이 틀리면 모든 클라이언트 에러 처리가 깨진다.
3. **채팅 폴링 제약 SM-C1:** 나중에 구현되지만, `idx_chat_messages_post_id_sent_at` 인덱스는 이 스토리에서 반드시 생성해야 한다.
4. **FK CASCADE 설정:** `posts → chat_messages`, `posts → likes`, `users → posts`, `users → reviews`, `users → chat_messages` 모두 ON DELETE CASCADE 필수.
5. **패키지 관리자:** FastAPI는 반드시 `uv` 사용 (pip/poetry 사용 금지).

### carrot-api: FastAPI 설정 패턴

**app/main.py 구조:**
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.shared.exceptions import AppException, app_exception_handler
from app.api.v1.router import router as api_router

app = FastAPI(title="carrot API", version="1.0.0")

# CORS: 3개 오리진 허용 (NFR-4)
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:3000",   # carrot-web 로컬
        "http://localhost:3001",   # carrot-admin 로컬
        # 배포 URL은 환경변수로 관리 (Story 6.5에서 추가)
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

app.add_exception_handler(AppException, app_exception_handler)
app.include_router(api_router, prefix="/api/v1")

@app.get("/health")
async def health_check():
    return {"status": "ok"}
```

**app/shared/exceptions.py:**
```python
from fastapi import Request
from fastapi.responses import JSONResponse

class AppException(Exception):
    def __init__(self, code: str, detail: str, status_code: int = 400):
        self.code = code
        self.detail = detail
        self.status_code = status_code

async def app_exception_handler(request: Request, exc: AppException):
    return JSONResponse(
        status_code=exc.status_code,
        content={"detail": exc.detail, "code": exc.code}
    )
```

**app/shared/schemas.py:**
```python
from typing import Generic, TypeVar, List
from pydantic import BaseModel

T = TypeVar("T")

class PaginatedResponse(BaseModel, Generic[T]):
    items: List[T]
    total: int
    page: int
    size: int
```

**app/core/database.py:**
```python
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker
from sqlalchemy.orm import DeclarativeBase
from app.core.config import settings

engine = create_async_engine(settings.DATABASE_URL, echo=True)
AsyncSessionLocal = async_sessionmaker(engine, expire_on_commit=False)

class Base(DeclarativeBase):
    pass

async def get_db():
    async with AsyncSessionLocal() as session:
        yield session
```

**app/core/config.py:**
```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    DATABASE_URL: str
    SUPABASE_URL: str
    SUPABASE_KEY: str
    JWT_SECRET: str
    JWT_ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 43200  # 30일

    class Config:
        env_file = ".env"

settings = Settings()
```

**app/core/dependencies.py:**
```python
from fastapi import Depends, Header
from app.core.security import verify_token
from app.shared.exceptions import AppException

async def get_current_user(authorization: str = Header(...)):
    if not authorization.startswith("Bearer "):
        raise AppException(code="UNAUTHORIZED", detail="인증이 필요합니다.", status_code=401)
    token = authorization.removeprefix("Bearer ")
    payload = verify_token(token)
    if not payload:
        raise AppException(code="UNAUTHORIZED", detail="유효하지 않은 토큰입니다.", status_code=401)
    return payload

async def require_admin(current_user = Depends(get_current_user)):
    if current_user.get("role") != "admin":
        raise AppException(code="ADMIN_REQUIRED", detail="관리자 권한이 없습니다.", status_code=403)
    return current_user
```

### DB 스키마 — 모델 네이밍 규칙

모든 테이블명은 `snake_case 복수형`, 컬럼명은 `snake_case`:

| 테이블 | 주요 컬럼 | FK / 인덱스 |
|--------|-----------|------------|
| `users` | id, email(UNIQUE), hashed_password, address, phone, role, is_active, manner_temp(FLOAT, DEFAULT 36.5) | — |
| `posts` | id, seller_email, title, description, price, is_free, status, trade_place, photos(ARRAY), buyer_email, view_count, is_draft, created_at, updated_at | seller_email → users.email, buyer_email → users.email |
| `chat_messages` | id, room_id(UUID), seller_email, buyer_email, post_id, message, is_read(DEFAULT FALSE), sent_at | post_id → posts.id CASCADE, 복합인덱스(post_id+sent_at) |
| `likes` | id, user_email, post_id, created_at | user_email → users.email CASCADE, post_id → posts.id CASCADE, UNIQUE(user_email, post_id) |
| `reviews` | id, post_id, writer_email, target_email, is_positive, tags(ARRAY), comment, created_at | post_id → posts.id CASCADE, UNIQUE(post_id, writer_email) |

**반드시 생성해야 할 인덱스:**
```python
# alembic 마이그레이션 파일 내
op.create_index(
    'idx_chat_messages_post_id_sent_at',
    'chat_messages',
    ['post_id', 'sent_at']
)
```

**SQLAlchemy 모델에서 CASCADE 설정:**
```python
# posts/models.py
from sqlalchemy import ForeignKey
from sqlalchemy.orm import relationship

class Post(Base):
    __tablename__ = "posts"
    seller_email = Column(String, ForeignKey("users.email", ondelete="CASCADE"), nullable=False)
    # chat_messages와 likes는 Post에 FK를 선언, CASCADE는 DB 레벨
```

### carrot-web / carrot-admin: 색상 토큰 (tailwind.config.ts)

```typescript
// tailwind.config.ts
import type { Config } from 'tailwindcss'

const config: Config = {
  // ... 기존 설정
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#FF7E36',
          hover: '#E86D28',
        },
        'manner-cold': '#3B82F6',    // 0~30℃ (파랑)
        'manner-warm': '#FF7E36',   // 36.5~50℃ (오렌지)
        'manner-hot': '#EF4444',    // 50~99℃ (빨강)
        'status-reserved': '#6B7280',
        'status-done': '#374151',
      },
    },
  },
}
export default config
```

### carrot-web / carrot-admin: Axios 인스턴스 패턴

```typescript
// src/lib/api/axios.ts
import axios from 'axios'

const apiClient = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000',
  headers: { 'Content-Type': 'application/json' },
})

// 요청 인터셉터: JWT 자동 주입
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 응답 인터셉터: 401 → 로그인 리다이렉트
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token')
      window.location.href = '/auth/login'
    }
    return Promise.reject(error)
  }
)

export default apiClient
```

### carrot-web / carrot-admin: TanStack Query 설정

```typescript
// src/lib/providers.tsx
'use client'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { useState } from 'react'

export function Providers({ children }: { children: React.ReactNode }) {
  const [queryClient] = useState(() => new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: 1000 * 60,  // 1분
        retry: 1,
      },
    },
  }))
  return <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>
}
```

**TanStack Query queryKey 컨벤션 (전 프로젝트 통일):**
```typescript
// 패턴: [리소스_문자열, 식별자?, 서브리소스?]
['posts']                         // 게시글 목록
['posts', postId]                 // 게시글 상세
['chats']                         // 채팅방 목록
['chats', roomId, 'messages']     // 채팅 메시지 (refetchInterval: 2500)
['users', 'me']                   // 내 프로필
['users', 'me', 'posts']          // 내 게시글
['chats', 'unread-count']         // 미읽음 배지 (refetchInterval: 2500)
```

### carrot-mobile: Flutter 설정 패턴

**pubspec.yaml 의존성:**
```yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  http: ^1.2.2
  go_router: ^14.6.2
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
  riverpod_generator: ^2.6.2
```

**lib/main.dart:**
```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'core/theme/app_theme.dart';
import 'shared/widgets/main_scaffold.dart';

void main() {
  runApp(const ProviderScope(child: CarrotApp()));
}

class CarrotApp extends StatelessWidget {
  const CarrotApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Carrot',
      theme: AppTheme.light,
      home: const MainScaffold(),
    );
  }
}
```

**lib/core/theme/app_theme.dart:**
```dart
import 'package:flutter/material.dart';
import 'app_colors.dart';

class AppTheme {
  static final light = ThemeData(
    useMaterial3: true,
    colorScheme: ColorScheme.fromSeed(
      seedColor: AppColors.primary,  // Color(0xFFFF7E36)
      brightness: Brightness.light,
    ),
  );
}
```

**lib/core/theme/app_colors.dart:**
```dart
import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFFF7E36);
  static const primaryHover = Color(0xFFE86D28);
  static const mannerCold = Color(0xFF3B82F6);   // 0~30℃
  static const mannerWarm = Color(0xFFFF7E36);   // 36.5~50℃
  static const mannerHot = Color(0xFFEF4444);    // 50~99℃
  static const statusReserved = Color(0xFF6B7280);
  static const statusDone = Color(0xFF374151);
}
```

**lib/core/api/api_client.dart:**
```dart
import 'dart:convert';
import 'package:http/http.dart' as http;
import 'api_endpoints.dart';

class ApiClient {
  final String? _token;
  const ApiClient({String? token}) : _token = token;

  Map<String, String> get _headers => {
    'Content-Type': 'application/json',
    if (_token != null) 'Authorization': 'Bearer $_token',
  };

  Future<dynamic> get(String path) async {
    final res = await http.get(Uri.parse('${ApiEndpoints.baseUrl}$path'), headers: _headers);
    return _handleResponse(res);
  }

  Future<dynamic> post(String path, {Map<String, dynamic>? body}) async {
    final res = await http.post(
      Uri.parse('${ApiEndpoints.baseUrl}$path'),
      headers: _headers,
      body: body != null ? jsonEncode(body) : null,
    );
    return _handleResponse(res);
  }

  Future<dynamic> patch(String path, {Map<String, dynamic>? body}) async {
    final res = await http.patch(
      Uri.parse('${ApiEndpoints.baseUrl}$path'),
      headers: _headers,
      body: body != null ? jsonEncode(body) : null,
    );
    return _handleResponse(res);
  }

  Future<dynamic> delete(String path) async {
    final res = await http.delete(Uri.parse('${ApiEndpoints.baseUrl}$path'), headers: _headers);
    return _handleResponse(res);
  }

  dynamic _handleResponse(http.Response res) {
    final decoded = jsonDecode(utf8.decode(res.bodyBytes));
    if (res.statusCode >= 400) {
      throw ApiException(
        statusCode: res.statusCode,
        code: decoded['code'] ?? 'UNKNOWN',
        detail: decoded['detail'] ?? '알 수 없는 오류',
      );
    }
    return decoded;
  }
}

class ApiException implements Exception {
  final int statusCode;
  final String code;
  final String detail;
  const ApiException({required this.statusCode, required this.code, required this.detail});
  @override
  String toString() => 'ApiException($statusCode, $code): $detail';
}
```

**lib/core/api/api_endpoints.dart:**
```dart
class ApiEndpoints {
  static const baseUrl = 'http://10.0.2.2:8000';  // Android 에뮬레이터용 localhost
  // iOS 실기기: 로컬 IP 사용
  static const auth = '/api/v1/auth';
  static const posts = '/api/v1/posts';
  static const chats = '/api/v1/chats';
  static const users = '/api/v1/users';
  static const reviews = '/api/v1/reviews';
}
```

**lib/shared/widgets/main_scaffold.dart:**
```dart
import 'package:flutter/material.dart';

class MainScaffold extends StatefulWidget {
  const MainScaffold({super.key});
  @override
  State<MainScaffold> createState() => _MainScaffoldState();
}

class _MainScaffoldState extends State<MainScaffold> {
  int _selectedIndex = 0;

  final _pages = const [
    Scaffold(body: Center(child: Text('홈 피드'))),    // Story 2.1에서 교체
    Scaffold(body: Center(child: Text('채팅'))),       // Story 3.3에서 교체
    Scaffold(body: Center(child: Text('나의 당근'))),  // Story 1.4에서 교체
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: _pages[_selectedIndex],
      bottomNavigationBar: NavigationBar(
        selectedIndex: _selectedIndex,
        onDestinationSelected: (i) => setState(() => _selectedIndex = i),
        destinations: const [
          NavigationDestination(icon: Icon(Icons.home_outlined), selectedIcon: Icon(Icons.home), label: '홈'),
          NavigationDestination(icon: Icon(Icons.chat_bubble_outline), selectedIcon: Icon(Icons.chat_bubble), label: '채팅'),
          NavigationDestination(icon: Icon(Icons.person_outline), selectedIcon: Icon(Icons.person), label: '나의당근'),
        ],
      ),
    );
  }
}
```

### .env.example 파일 내용

**carrot-api/.env.example:**
```
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/carrot
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key
JWT_SECRET=your-super-secret-key-min-32-chars
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200
```

**carrot-web/.env.example 및 carrot-admin/.env.example:**
```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

### 디렉토리 구조 최종 검증 포인트

Task 완료 후 반드시 확인:
- `carrot-api/app/main.py` → `GET /health` 200 응답 확인 (`uvicorn app.main:app --reload`)
- `carrot-web/` → `npm run dev` 후 http://localhost:3000 정상 렌더링
- `carrot-admin/` → `npm run dev --port 3001` 후 http://localhost:3001 정상 렌더링
- `carrot-mobile/` → `flutter run` 후 3탭 NavigationBar 렌더링 확인
- Alembic 마이그레이션 파일에 5개 테이블 + `idx_chat_messages_post_id_sent_at` 존재 확인

### 후속 스토리 의존성

이 스토리가 생성하는 기반 위에 모든 후속 스토리가 구축된다:
- **Story 1.2~1.4:** `app/features/users/` 도메인 + `alembic upgrade head` 실행
- **Story 2.1 이후:** `app/features/posts/` 도메인 + `carrot-web`의 API 훅
- **모든 Flutter 스토리:** `ApiClient` + `ProviderScope` + 색상 토큰 재사용
- **Story 6.5:** Railway 환경변수가 `.env.example` 기반으로 설정됨

### Project Structure Notes

- 모노레포 루트 = `C:\Users\HEMICOLON\Documents\WORK\VIBE\project\carrot\` (현재 작업 디렉토리)
- `carrot-api/`, `carrot-web/`, `carrot-admin/`, `carrot-mobile/` 모두 이 아래에 생성
- Flutter 프로젝트명: `carrot_mobile` (Dart 패키지명 규칙: snake_case), 디렉토리명: `carrot-mobile` (kebab-case)
- Next.js: `src/` 하위 구조 (`--src-dir` 플래그 필수)
- FastAPI: `app/` 하위 도메인 기반 구조 (`features/{domain}/`)

### References

- [Source: architecture.md#플랫폼 3: Flutter] — `flutter create` 커맨드 및 플랫폼 타겟
- [Source: architecture.md#플랫폼 1 & 2: Next.js] — `create-next-app` 커맨드 및 shadcn 초기화
- [Source: architecture.md#플랫폼 4: FastAPI] — 도메인 기반 구조 및 uv 패키지 관리
- [Source: architecture.md#Data Architecture] — FK CASCADE 관계 및 Alembic 전략
- [Source: architecture.md#Naming Patterns] — 네이밍 컨벤션 (snake_case, PascalCase)
- [Source: architecture.md#Enforcement Guidelines] — 9개 강제 규칙
- [Source: epics.md#Story 1.1] — 인수 조건 및 AR-1~AR-4, AR-8~AR-10 커버리지
- [Source: epics.md#Requirements Inventory AR-3] — 5개 테이블 + 인덱스 명세
- [Source: epics.md#UX Design Requirements UX-DR1] — 색상 토큰 (#FF7E36 등)
- [Source: epics.md#UX Design Requirements UX-DR13] — Flutter 3탭 NavigationBar

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

- Tailwind v4 (Next.js 16 기본)는 `tailwind.config.ts` 없음 → `globals.css`의 `@theme inline`과 `:root` CSS 변수로 색상 토큰 등록
- uv가 없어 `pip install uv`로 먼저 설치 후 venv 구성
- Flutter 프로젝트명 `carrot_mobile`(Dart 패키지명 제약) → 디렉토리 `carrot-mobile`로 rename
- `dart analyze` 결과 `prefer_initializing_formals` 1건 → ignore 주석으로 처리 (private field 패턴)
- FastAPI import 테스트: `/health` 라우트 포함 5개 라우트 정상 등록 확인

### Completion Notes List

- ✅ AC#1: 모노레포 루트에 carrot-api, carrot-web, carrot-admin, carrot-mobile 4개 프로젝트 생성
- ✅ AC#2: FastAPI + SQLAlchemy 2.0 Async + Alembic 설치, `GET /health` 라우트 등록, import 검증 완료
- ✅ AC#3: carrot-web, carrot-admin Next.js 16 + TypeScript + Tailwind v4 + shadcn/ui 초기화
- ✅ AC#4: Flutter 3.44.0 carrot-mobile, ProviderScope + NavigationBar 3탭(홈/채팅/나의당근) 구성
- ✅ AC#5: Tailwind v4 방식으로 globals.css에 carrot 색상 토큰 등록 (primary #FF7E36, manner-cold/warm/hot, status-reserved/done)
- ✅ AC#6: AppTheme.light — `ColorScheme.fromSeed(seedColor: Color(0xFFFF7E36))` 적용
- ✅ AC#7: axios.ts (JWT 인터셉터 + 401 리다이렉트), providers.tsx (QueryClient) carrot-web, carrot-admin 각각 생성
- ✅ AC#8: ProviderScope 최상위 래핑, ApiClient (http 패키지 래퍼) + ApiEndpoints 상수 클래스 생성
- ✅ AC#9: 5개 테이블(users, posts, chat_messages, likes, reviews) + FK CASCADE + idx_chat_messages_post_id_sent_at 복합 인덱스 마이그레이션 파일 생성
- ✅ AC#10: carrot-api, carrot-web, carrot-admin 각각 .env.example 생성

### File List

**carrot-api/**
- `pyproject.toml`
- `.env.example`
- `alembic.ini`
- `alembic/env.py`
- `alembic/script.py.mako`
- `alembic/versions/001_initial_schema.py`
- `app/__init__.py`
- `app/main.py`
- `app/core/__init__.py`
- `app/core/config.py`
- `app/core/database.py`
- `app/core/security.py`
- `app/core/dependencies.py`
- `app/api/__init__.py`
- `app/api/v1/__init__.py`
- `app/api/v1/router.py`
- `app/features/__init__.py`
- `app/features/users/__init__.py`
- `app/features/users/models.py`
- `app/features/posts/__init__.py`
- `app/features/posts/models.py` (Post + Like)
- `app/features/chats/__init__.py`
- `app/features/chats/models.py`
- `app/features/reviews/__init__.py`
- `app/features/reviews/models.py`
- `app/features/admin/__init__.py`
- `app/shared/__init__.py`
- `app/shared/exceptions.py`
- `app/shared/schemas.py`
- `app/shared/utils.py`
- `tests/__init__.py`
- `tests/unit/__init__.py`
- `tests/integration/__init__.py`

**carrot-web/** (create-next-app 생성 파일 외 추가/수정)
- `src/app/globals.css` (carrot 색상 토큰 추가)
- `src/app/layout.tsx` (Providers 래핑, 메타데이터 한국어)
- `src/lib/api/axios.ts`
- `src/lib/providers.tsx`
- `.env.example`

**carrot-admin/** (create-next-app 생성 파일 외 추가/수정)
- `src/app/globals.css` (carrot 색상 토큰 추가)
- `src/app/layout.tsx` (Providers 래핑, 메타데이터 한국어)
- `src/lib/api/axios.ts`
- `src/lib/providers.tsx`
- `.env.example`

**carrot-mobile/** (flutter create 생성 파일 외 추가/수정)
- `pubspec.yaml` (의존성 추가)
- `lib/main.dart` (CarrotApp, ProviderScope)
- `lib/core/theme/app_colors.dart`
- `lib/core/theme/app_theme.dart`
- `lib/core/api/api_client.dart`
- `lib/core/api/api_endpoints.dart`
- `lib/shared/widgets/main_scaffold.dart`

**루트**
- `.gitignore`
