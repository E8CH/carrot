# Story 4.2: 구매자 확정 & 거래완료 플로우

Status: done

## Story

As a 판매자,
I want to mark a trade as complete and confirm the buyer,
So that the transaction is officially recorded and both parties can leave reviews.

## Acceptance Criteria

1. 판매자가 거래완료 버튼을 누르면 TradeCompleteSheet(바텀시트/다이얼로그)가 열리고, 해당 게시글에서 채팅한 구매자 목록(`GET /api/v1/posts/{post_id}/buyers`)이 표시된다
2. 판매자가 구매자를 선택하고 확정하면 `PATCH /api/v1/posts/{post_id}/complete` (`{buyer_email}`)를 호출하여 게시글 `status`=`거래완료`, `buyer_email` 저장, 거래완료 체크 애니메이션(1초) → 후기 작성 CTA 자동 노출
3. 거래완료로 변경된 게시글에 `PATCH /api/v1/posts/{post_id}/status`를 다시 호출하면 `{"detail": "거래완료된 게시글은 상태를 변경할 수 없습니다.", "code": "TRADE_ALREADY_COMPLETED"}` 422 에러 반환
4. 타인이 `PATCH /api/v1/posts/{post_id}/complete`를 호출하면 403 `FORBIDDEN` 에러 반환

## Tasks / Subtasks

- [x] Task 1: carrot-api — service.py 에러 코드 수정 (AC: #3)
  - Story 4.1에서 `STATUS_LOCKED`로 구현된 거래완료 역전환 방지 에러를 spec 그대로 `TRADE_ALREADY_COMPLETED`·`"거래완료된 게시글은 상태를 변경할 수 없습니다."` 로 수정
  - `test_change_post_status_locked` 테스트의 assert도 함께 수정

- [x] Task 2: carrot-api — schemas.py 수정 (AC: #1, #2)
  - `CompletePostRequest(buyer_email: str)` 추가 (model_validator로 비어있으면 ValidationError)
  - `BuyerItem(buyer_email: str)` + `BuyersResponse(buyers: list[BuyerItem])` 추가

- [x] Task 3: carrot-api — repository.py 수정 (AC: #1)
  - `get_post_buyers(db, post_id)`: `SELECT DISTINCT buyer_email FROM chat_messages WHERE post_id = post_id AND buyer_email IS NOT NULL`

- [x] Task 4: carrot-api — service.py 수정 (AC: #1, #2, #4)
  - `get_post_buyers(db, seller_email, post_id)`: 소유권 검증 후 repository 호출, BuyersResponse 반환
  - `complete_post(db, seller_email, post_id, buyer_email)`: 소유권 검증 → 이미 거래완료 시 TRADE_ALREADY_COMPLETED(422) → post.status='거래완료', post.buyer_email=buyer_email, commit+refresh → PostDetailResponse 반환

- [x] Task 5: carrot-api — router.py 수정 (AC: #1, #2, #4)
  - `GET /{post_id}/buyers` (JWT 필요) — `/{post_id}` 앞에 등록 (literal path 우선)
  - `PATCH /{post_id}/complete` (JWT 필요) — `/{post_id}` 앞, `/status` 뒤에 등록

- [x] Task 6: carrot-api — 단위 테스트 추가 (AC: #2, #3, #4)
  - `test_complete_post_success`: 정상 거래완료
  - `test_complete_post_already_completed`: 이미 거래완료된 게시글 → TRADE_ALREADY_COMPLETED(422)
  - `test_complete_post_forbidden`: 타인 → FORBIDDEN(403)

- [x] Task 7: carrot-web — posts.ts 수정
  - `getPostBuyers(id)`: `GET /api/v1/posts/{id}/buyers`
  - `completePost(id, buyerEmail)`: `PATCH /api/v1/posts/{id}/complete`

- [x] Task 8: carrot-web — TradeCompleteSheet.tsx 신규 생성 (AC: #1, #2)
  - `src/components/posts/TradeCompleteSheet.tsx`
  - Props: `postId, isOpen, onClose, onComplete(buyerEmail)`
  - 구매자 목록 로딩 → 선택 → 확정 버튼 → 체크 애니메이션(1초) → 후기 CTA

- [x] Task 9: carrot-web — /posts/[id]/page.tsx 수정 (AC: #1, #2)
  - isOwner 바에 "거래완료" 버튼 추가 (`post.status !== '거래완료'`일 때만)
  - TradeCompleteSheet 상태관리(`showTradeSheet`) + 완료 콜백 후 `queryClient.invalidateQueries`

- [x] Task 10: carrot-mobile — posts_repository.dart 수정
  - `getPostBuyers(id)`: GET postBuyers(id)
  - `completePost(id, buyerEmail)`: PATCH postComplete(id) body=`{buyer_email}`

- [x] Task 11: carrot-mobile — trade_complete_sheet.dart 신규 생성 (AC: #1, #2)
  - `lib/shared/widgets/trade_complete_sheet.dart`
  - StatefulWidget: 구매자 목록 조회 → 선택 → 확정 → 체크 애니메이션(1초) → 후기 CTA 노출

- [x] Task 12: carrot-mobile — post_detail_screen.dart 수정 (AC: #1, #2)
  - isOwner 바에 "거래완료" 버튼 추가 (`status != '거래완료'`일 때만)
  - `_showTradeCompleteSheet()` → 바텀시트 표시 → 완료 시 `_loadPost()` 재호출

## Dev Notes

### Task 1: 에러 코드 즉시 수정 (CRITICAL)

Story 4.1 CR 패치에서 `STATUS_LOCKED`로 구현됐으나, epics.md에서 Story 4.2 AC3은 `TRADE_ALREADY_COMPLETED`를 명시한다. **Task 1을 가장 먼저 수행할 것.**

**service.py `change_post_status` 수정:**
```python
if post.status == '거래완료':
    raise AppException(
        code="TRADE_ALREADY_COMPLETED",
        detail="거래완료된 게시글은 상태를 변경할 수 없습니다.",
        status_code=422,
    )
```

**test_posts_service.py 수정:**
```python
assert exc_info.value.code == "TRADE_ALREADY_COMPLETED"
```

### Task 2: schemas.py

```python
class CompletePostRequest(BaseModel):
    buyer_email: str = Field(min_length=1)

class BuyerItem(BaseModel):
    buyer_email: str

class BuyersResponse(BaseModel):
    buyers: list[BuyerItem]
```

### Task 3: repository.py — get_post_buyers

```python
from sqlalchemy import distinct

async def get_post_buyers(db: AsyncSession, post_id: int) -> list[str]:
    stmt = (
        select(distinct(ChatMessage.buyer_email))
        .where(
            ChatMessage.post_id == post_id,
            ChatMessage.buyer_email.is_not(None),
        )
    )
    return list((await db.execute(stmt)).scalars().all())
```

`ChatMessage`는 `app.features.chats.models`에 있음. `repository.py`에 이미 `from app.features.chats.models import ChatMessage` 임포트 존재 → 재사용.

### Task 4: service.py — get_post_buyers + complete_post

```python
async def get_post_buyers(
    db: AsyncSession,
    seller_email: str,
    post_id: int,
) -> BuyersResponse:
    await _get_owned_post(db, post_id, seller_email)  # 소유권 검증 (404/403)
    buyers = await repository.get_post_buyers(db, post_id)
    return BuyersResponse(buyers=[BuyerItem(buyer_email=e) for e in buyers])


async def complete_post(
    db: AsyncSession,
    seller_email: str,
    post_id: int,
    buyer_email: str,
) -> PostDetailResponse:
    post = await _get_owned_post(db, post_id, seller_email)
    if post.status == '거래완료':
        raise AppException(
            code="TRADE_ALREADY_COMPLETED",
            detail="거래완료된 게시글은 상태를 변경할 수 없습니다.",
            status_code=422,
        )
    post.status = '거래완료'
    post.buyer_email = buyer_email
    try:
        await db.commit()
        await db.refresh(post)
    except IntegrityError as e:
        await db.rollback()
        raise AppException(
            code="COMPLETE_FAILED",
            detail="거래완료 처리에 실패했습니다.",
            status_code=422,
        ) from e
    return PostDetailResponse(
        id=post.id,
        seller_email=post.seller_email,
        title=post.title,
        description=post.description,
        price=post.price,
        is_free=post.is_free,
        status=post.status,
        trade_place=post.trade_place,
        photos=post.photos,
        view_count=post.view_count,
        created_at=post.created_at,
        updated_at=post.updated_at,
        chat_count=0,
        like_count=0,
        manner_temp=36.5,
        is_liked=False,
    )
```

임포트 추가: `BuyerItem, BuyersResponse, CompletePostRequest`

### Task 5: router.py — 엔드포인트 등록 순서

```python
# /{post_id}/buyers는 /{post_id} 앞에 등록 (literal > path param)
@router.get("/{post_id}/buyers", response_model=BuyersResponse)
async def get_post_buyers(
    post_id: int = Path(ge=1),
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> BuyersResponse:
    seller_email = current_user.get("sub")
    if not seller_email:
        raise AppException(code="UNAUTHORIZED", detail="유효하지 않은 토큰입니다.", status_code=401)
    return await service.get_post_buyers(db, seller_email, post_id)


@router.patch("/{post_id}/complete", response_model=PostDetailResponse)
async def complete_post(
    request: CompletePostRequest,
    post_id: int = Path(ge=1),
    current_user: dict = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> PostDetailResponse:
    seller_email = current_user.get("sub")
    if not seller_email:
        raise AppException(code="UNAUTHORIZED", detail="유효하지 않은 토큰입니다.", status_code=401)
    return await service.complete_post(db, seller_email, post_id, request.buyer_email)
```

**등록 순서 (router.py 현재):**
```
GET  /               → list_posts
POST /upload-url     → get_upload_url
GET  /draft          → get_draft  ← literal 먼저 (중요)
POST /draft          → save_draft
GET  /{post_id}      → get_post
POST /{post_id}/like → toggle_like
PATCH /{post_id}/status   → change_post_status  ← 이미 있음
NEW: GET /{post_id}/buyers   ← 추가
NEW: PATCH /{post_id}/complete ← 추가
PATCH /{post_id}     → patch_post  ← /{post_id}/xxx 모두 /{post_id} 앞에 있어야 함
DELETE /{post_id}    → delete_post
POST /               → create_post
```

`/{post_id}/buyers`와 `/{post_id}/complete`를 `/{post_id}` (PATCH) 앞에 배치. FastAPI는 경로 세그먼트 수로 구분하므로 실제 충돌은 없지만 관행상 literal 먼저.

### Task 6: 단위 테스트

```python
@pytest.mark.asyncio
async def test_complete_post_success():
    """정상 거래완료: status='거래완료', buyer_email 저장"""
    db = AsyncMock()
    post = make_mock_post_full()
    post.status = "예약중"
    post.buyer_email = None

    with (
        patch("app.features.posts.service.repository.get_post_by_id", new=AsyncMock(return_value=post)),
        patch.object(db, "commit", new=AsyncMock()),
        patch.object(db, "refresh", new=AsyncMock()),
    ):
        result = await complete_post(db, "seller@example.com", 1, "buyer@example.com")

    assert result.status == "거래완료"
    assert post.status == "거래완료"
    assert post.buyer_email == "buyer@example.com"


@pytest.mark.asyncio
async def test_complete_post_already_completed():
    """이미 거래완료된 게시글 → TRADE_ALREADY_COMPLETED (422)"""
    db = AsyncMock()
    post = make_mock_post_full()
    post.status = "거래완료"

    with patch("app.features.posts.service.repository.get_post_by_id", new=AsyncMock(return_value=post)):
        with pytest.raises(AppException) as exc_info:
            await complete_post(db, "seller@example.com", 1, "buyer@example.com")

    assert exc_info.value.code == "TRADE_ALREADY_COMPLETED"
    assert exc_info.value.status_code == 422


@pytest.mark.asyncio
async def test_complete_post_forbidden():
    """타인 거래완료 시도 → FORBIDDEN (403)"""
    db = AsyncMock()
    post = make_mock_post_full()  # seller_email = "seller@example.com"

    with patch("app.features.posts.service.repository.get_post_by_id", new=AsyncMock(return_value=post)):
        with pytest.raises(AppException) as exc_info:
            await complete_post(db, "other@example.com", 1, "buyer@example.com")

    assert exc_info.value.code == "FORBIDDEN"
    assert exc_info.value.status_code == 403
```

`complete_post` 임포트 추가 필요.

### Task 7: carrot-web posts.ts

```typescript
getPostBuyers: (id: number): Promise<{ buyers: Array<{ buyer_email: string }> }> =>
  apiClient.get(`/api/v1/posts/${id}/buyers`).then((r) => r.data),

completePost: (id: number, buyerEmail: string): Promise<PostDetailResponse> =>
  apiClient
    .patch<PostDetailResponse>(`/api/v1/posts/${id}/complete`, { buyer_email: buyerEmail })
    .then((r) => r.data),
```

### Task 8: carrot-web TradeCompleteSheet.tsx

파일 위치: `carrot-web/src/components/posts/TradeCompleteSheet.tsx`

UX 플로우: 구매자 목록 표시 → 선택 → "거래완료 확정" 버튼 → 체크 애니메이션(1초) → "후기 작성" CTA (Story 4.3으로 연결, 현재는 버튼 노출만)

```tsx
'use client'

import { useEffect, useState } from 'react'
import { postsApi } from '@/lib/api/posts'
import { Button } from '@/components/ui/button'

interface Props {
  postId: number
  isOpen: boolean
  onClose: () => void
  onComplete: () => void
}

type Phase = 'select' | 'animating' | 'done'

export function TradeCompleteSheet({ postId, isOpen, onClose, onComplete }: Props) {
  const [buyers, setBuyers] = useState<string[]>([])
  const [selected, setSelected] = useState<string | null>(null)
  const [phase, setPhase] = useState<Phase>('select')
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState('')

  useEffect(() => {
    if (!isOpen) { setPhase('select'); setSelected(null); setError(''); return }
    postsApi.getPostBuyers(postId)
      .then((r) => setBuyers(r.buyers.map((b) => b.buyer_email)))
      .catch(() => setError('구매자 목록을 불러오지 못했습니다.'))
  }, [isOpen, postId])

  const handleConfirm = async () => {
    if (!selected || loading) return
    setLoading(true)
    setError('')
    try {
      await postsApi.completePost(postId, selected)
      setPhase('animating')
      setTimeout(() => { setPhase('done') }, 1000)
    } catch {
      setError('거래완료 처리에 실패했습니다.')
    } finally {
      setLoading(false)
    }
  }

  const handleReviewLater = () => { onComplete(); onClose() }

  if (!isOpen) return null

  return (
    <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/40">
      <div className="bg-white w-full max-w-lg rounded-t-2xl p-6 space-y-4">
        {phase === 'select' && (
          <>
            <h2 className="text-base font-semibold">거래한 상대를 선택해 주세요</h2>
            {error && <p className="text-sm text-red-500">{error}</p>}
            {buyers.length === 0 && !error && (
              <p className="text-sm text-gray-400">채팅한 구매자가 없습니다.</p>
            )}
            <div className="space-y-2 max-h-48 overflow-y-auto">
              {buyers.map((email) => (
                <button
                  key={email}
                  onClick={() => setSelected(email)}
                  className={`w-full text-left px-4 py-3 rounded-lg border text-sm transition-colors ${
                    selected === email
                      ? 'border-orange-500 bg-orange-50 text-orange-700'
                      : 'border-gray-200 hover:bg-gray-50'
                  }`}
                >
                  {email}
                </button>
              ))}
            </div>
            <div className="flex gap-2 pt-2">
              <Button variant="outline" className="flex-1" onClick={onClose}>취소</Button>
              <Button
                className="flex-1"
                style={{ backgroundColor: selected ? '#FF7E36' : undefined }}
                disabled={!selected || loading}
                onClick={handleConfirm}
              >
                {loading ? '처리 중...' : '거래완료 확정'}
              </Button>
            </div>
          </>
        )}

        {phase === 'animating' && (
          <div className="flex flex-col items-center py-8 gap-3">
            <div className="w-16 h-16 rounded-full bg-green-100 flex items-center justify-center text-4xl animate-bounce">
              ✅
            </div>
            <p className="text-base font-semibold text-gray-800">거래가 완료됐어요!</p>
          </div>
        )}

        {phase === 'done' && (
          <div className="flex flex-col items-center py-4 gap-4">
            <div className="w-16 h-16 rounded-full bg-green-100 flex items-center justify-center text-4xl">✅</div>
            <p className="text-base font-semibold">거래가 완료됐어요!</p>
            <p className="text-sm text-gray-500">상대방에게 후기를 남겨 매너온도를 올려보세요.</p>
            <Button
              className="w-full"
              style={{ backgroundColor: '#FF7E36' }}
              onClick={handleReviewLater}
            >
              후기 작성하기 (Story 4.3)
            </Button>
            <button className="text-sm text-gray-400 underline" onClick={handleReviewLater}>
              나중에 하기
            </button>
          </div>
        )}
      </div>
    </div>
  )
}
```

### Task 9: carrot-web /posts/[id]/page.tsx

isOwner 바에 "거래완료" 버튼 + TradeCompleteSheet 통합:

```tsx
// 상태 추가
const [showTradeSheet, setShowTradeSheet] = useState(false)

// isOwner 바 수정 — 기존 statusError, statusLoading, 수정, 삭제 버튼 유지하면서 추가
{isOwner && (
  <>
    {post.status !== '거래완료' && (
      <>
        <Button variant="outline" onClick={() => setShowTradeSheet(true)}>거래완료</Button>
        <Button
          variant="outline"
          onClick={() => handleStatusChange(post.status === '판매중' ? '예약중' : '판매중')}
          disabled={statusLoading}
        >
          {post.status === '판매중' ? '예약중' : '판매중'}
        </Button>
      </>
    )}
    <Button variant="outline" onClick={() => router.push(`/posts/${post.id}/edit`)}>수정</Button>
    <Button variant="outline" onClick={() => setShowDeleteDialog(true)}>삭제</Button>
  </>
)}

// JSX 최하단에 TradeCompleteSheet 추가
<TradeCompleteSheet
  postId={postId}
  isOpen={showTradeSheet}
  onClose={() => setShowTradeSheet(false)}
  onComplete={() => {
    setShowTradeSheet(false)
    queryClient.invalidateQueries({ queryKey: ['post', postId] })
  }}
/>
```

**Import 추가:** `import { TradeCompleteSheet } from '@/components/posts/TradeCompleteSheet'`

### Task 10: carrot-mobile posts_repository.dart

```dart
Future<List<String>> getPostBuyers(int id) async {
  final result = await _client.get(ApiEndpoints.postBuyers(id));
  final data = result as Map<String, dynamic>;
  final buyers = (data['buyers'] as List?) ?? [];
  return buyers
      .map((b) => (b as Map<String, dynamic>)['buyer_email'] as String)
      .toList();
}

Future<Map<String, dynamic>> completePost(int id, String buyerEmail) async {
  final result = await _client.patch(
    ApiEndpoints.postComplete(id),
    body: {'buyer_email': buyerEmail},
  );
  return result as Map<String, dynamic>;
}
```

**api_endpoints.dart에 추가:**
```dart
static String postBuyers(int id) => '/api/v1/posts/$id/buyers';
static String postComplete(int id) => '/api/v1/posts/$id/complete';
```

### Task 11: carrot-mobile trade_complete_sheet.dart

파일 위치: `carrot-mobile/lib/shared/widgets/trade_complete_sheet.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../core/providers/api_client_provider.dart';
import '../../features/posts/data/posts_repository.dart';

class TradeCompleteSheet extends ConsumerStatefulWidget {
  final int postId;
  final VoidCallback onComplete;
  const TradeCompleteSheet({super.key, required this.postId, required this.onComplete});

  @override
  ConsumerState<TradeCompleteSheet> createState() => _TradeCompleteSheetState();
}

class _TradeCompleteSheetState extends ConsumerState<TradeCompleteSheet> {
  List<String> _buyers = [];
  String? _selected;
  bool _loading = false;
  bool _animating = false;
  bool _done = false;
  String? _error;

  @override
  void initState() {
    super.initState();
    _loadBuyers();
  }

  Future<void> _loadBuyers() async {
    try {
      final client = ref.read(apiClientProvider);
      final repo = PostsRepository(client);
      final buyers = await repo.getPostBuyers(widget.postId);
      if (mounted) setState(() => _buyers = buyers);
    } catch (_) {
      if (mounted) setState(() => _error = '구매자 목록을 불러오지 못했습니다.');
    }
  }

  Future<void> _confirm() async {
    if (_selected == null || _loading) return;
    final messenger = ScaffoldMessenger.of(context);
    if (mounted) setState(() => _loading = true);
    try {
      final client = ref.read(apiClientProvider);
      final repo = PostsRepository(client);
      await repo.completePost(widget.postId, _selected!);
      if (mounted) setState(() { _loading = false; _animating = true; });
      await Future.delayed(const Duration(seconds: 1));
      if (mounted) setState(() { _animating = false; _done = true; });
    } catch (_) {
      if (mounted) {
        setState(() => _loading = false);
        messenger.showSnackBar(const SnackBar(content: Text('거래완료 처리에 실패했습니다.')));
      }
    }
  }

  @override
  Widget build(BuildContext context) {
    if (_animating) {
      return _buildAnimating();
    }
    if (_done) {
      return _buildDone();
    }
    return _buildSelect();
  }

  Widget _buildSelect() {
    return Padding(
      padding: const EdgeInsets.all(24),
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          const Text('거래한 상대를 선택해 주세요', style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold)),
          const SizedBox(height: 16),
          if (_error != null) Text(_error!, style: const TextStyle(color: Colors.red, fontSize: 13)),
          if (_buyers.isEmpty && _error == null)
            const Text('채팅한 구매자가 없습니다.', style: TextStyle(color: Colors.grey)),
          ...(_buyers.map((email) => ListTile(
                title: Text(email, style: const TextStyle(fontSize: 14)),
                trailing: _selected == email ? const Icon(Icons.check, color: Color(0xFFFF7E36)) : null,
                onTap: () => setState(() => _selected = email),
                shape: RoundedRectangleBorder(
                  side: BorderSide(
                    color: _selected == email ? const Color(0xFFFF7E36) : Colors.grey.shade200,
                  ),
                  borderRadius: BorderRadius.circular(8),
                ),
              ))),
          const SizedBox(height: 16),
          Row(children: [
            Expanded(
              child: OutlinedButton(
                onPressed: () => Navigator.pop(context),
                child: const Text('취소'),
              ),
            ),
            const SizedBox(width: 8),
            Expanded(
              child: ElevatedButton(
                onPressed: (_selected != null && !_loading) ? _confirm : null,
                style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFFFF7E36), foregroundColor: Colors.white),
                child: Text(_loading ? '처리 중...' : '거래완료 확정'),
              ),
            ),
          ]),
        ],
      ),
    );
  }

  Widget _buildAnimating() {
    return const Padding(
      padding: EdgeInsets.all(48),
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          Text('✅', style: TextStyle(fontSize: 64)),
          SizedBox(height: 16),
          Text('거래가 완료됐어요!', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
        ],
      ),
    );
  }

  Widget _buildDone() {
    return Padding(
      padding: const EdgeInsets.all(24),
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          const Text('✅', style: TextStyle(fontSize: 64)),
          const SizedBox(height: 12),
          const Text('거래가 완료됐어요!', style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold)),
          const SizedBox(height: 8),
          const Text('상대방에게 후기를 남겨 매너온도를 올려보세요.', style: TextStyle(color: Colors.grey, fontSize: 13)),
          const SizedBox(height: 20),
          SizedBox(
            width: double.infinity,
            child: ElevatedButton(
              onPressed: () { Navigator.pop(context); widget.onComplete(); },
              style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFFFF7E36), foregroundColor: Colors.white),
              child: const Text('후기 작성하기 (Story 4.3)'),
            ),
          ),
          TextButton(
            onPressed: () { Navigator.pop(context); widget.onComplete(); },
            child: const Text('나중에 하기', style: TextStyle(color: Colors.grey)),
          ),
        ],
      ),
    );
  }
}
```

### Task 12: carrot-mobile post_detail_screen.dart

isOwner 바에 거래완료 버튼 + showModalBottomSheet 연결:

```dart
// isOwner 바 변경 (기존 상태변경+수정+삭제 버튼 유지)
_isOwner
    ? Row(children: [
        if (status != '거래완료') ...[
          // 거래완료 버튼 (신규)
          OutlinedButton(
            onPressed: _showTradeCompleteSheet,
            style: OutlinedButton.styleFrom(foregroundColor: const Color(0xFFFF7E36), side: const BorderSide(color: Color(0xFFFF7E36))),
            child: const Text('거래완료'),
          ),
          const SizedBox(width: 8),
          // 기존 상태변경 버튼 유지
          OutlinedButton(
            onPressed: _statusLoading ? null : () => _changeStatus(status == '판매중' ? '예약중' : '판매중'),
            child: Text(status == '판매중' ? '예약중' : '판매중'),
          ),
          const SizedBox(width: 8),
        ],
        Expanded(child: OutlinedButton(onPressed: ..., child: const Text('수정'))),
        const SizedBox(width: 8),
        Expanded(child: OutlinedButton(onPressed: ..., child: const Text('삭제'))),
      ])
    : ...
```

`_showTradeCompleteSheet` 메서드:
```dart
void _showTradeCompleteSheet() {
  showModalBottomSheet(
    context: context,
    isScrollControlled: true,
    shape: const RoundedRectangleBorder(
      borderRadius: BorderRadius.vertical(top: Radius.circular(20)),
    ),
    builder: (_) => TradeCompleteSheet(
      postId: widget.postId,
      onComplete: _loadPost,
    ),
  );
}
```

**Import 추가:** `import '../../../shared/widgets/trade_complete_sheet.dart';`

### DB 스키마 확인 (마이그레이션 불필요)

`Post` 모델에 이미 `buyer_email` 컬럼 존재:
```python
buyer_email: Mapped[str | None] = mapped_column(
    String(255), ForeignKey("users.email", ondelete="SET NULL"), nullable=True
)
```
Alembic 마이그레이션 불필요. 컬럼은 Story 1.1에서 이미 생성됨.

### 기존 코드 보존 필수

- `posts/router.py` 기존 엔드포인트 순서 유지 (upload-url, draft 먼저)
- `post_detail_screen.dart`의 `_changeStatus`, `_confirmDelete`, `_handleChat`, `_toggleLike` 로직 유지
- `/posts/[id]/page.tsx`의 기존 `statusError`, `showDeleteDialog`, `isLiked` 로직 유지

### References

- [Source: epics.md#Story 4.2] — AC 전문
- [Source: epics.md#FR-15, FR-16] — 판매자 본인만, 구매자 확정 필수
- [Source: models.py#Post] — buyer_email 이미 존재, status String(10)
- [Source: architecture.md#Structure Patterns] — TradeCompleteSheet 위치: `components/posts/`, `shared/widgets/`
- [Source: ux-design-specification.md#UJ-1] — 체크 애니메이션 1초, 후기 CTA 자동 노출
- [Source: ux-design-specification.md#Color System] — success #10B981 (체크 배경)
- [Source: chats/repository.py] — ChatMessage 모델 참조 패턴
- [Source: 4-1-거래-상태-변경.md#Dev Notes] — STATUS_LOCKED → TRADE_ALREADY_COMPLETED 변경 필요

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

### Completion Notes List

- **Task 1**: STATUS_LOCKED → TRADE_ALREADY_COMPLETED 에러 코드 수정, 테스트 업데이트
- **Task 2**: CompletePostRequest, BuyerItem, BuyersResponse 스키마 추가
- **Task 3**: repository.get_post_buyers() — DISTINCT buyer_email 쿼리
- **Task 4**: service.get_post_buyers(), complete_post() 구현 — 소유권 검증, 거래완료 상태 보호
- **Task 5**: GET /{post_id}/buyers, PATCH /{post_id}/complete 라우터 등록
- **Task 6**: 26/26 테스트 통과 (test_complete_post_success/already_completed/forbidden 추가)
- **Task 7**: posts.ts에 getPostBuyers, completePost API 함수 추가
- **Task 8**: TradeCompleteSheet.tsx 신규 — select→animating(1s)→done 3단계 플로우
- **Task 9**: page.tsx에 거래완료 버튼, showTradeSheet 상태, TradeCompleteSheet 통합
- **Task 10**: posts_repository.dart에 getPostBuyers, completePost 추가
- **Task 11**: trade_complete_sheet.dart 신규 — ConsumerStatefulWidget, 동일 3단계 플로우
- **Task 12**: post_detail_screen.dart에 거래완료 버튼 + _showTradeCompleteSheet 추가, api_endpoints.dart에 postBuyers 추가

### File List

- `carrot-api/app/features/posts/service.py` (수정: STATUS_LOCKED→TRADE_ALREADY_COMPLETED, complete_post, get_post_buyers 추가)
- `carrot-api/app/features/posts/schemas.py` (수정: CompletePostRequest, BuyerItem, BuyersResponse 추가)
- `carrot-api/app/features/posts/repository.py` (수정: get_post_buyers 추가)
- `carrot-api/app/features/posts/router.py` (수정: GET buyers, PATCH complete 추가)
- `carrot-api/tests/unit/features/test_posts_service.py` (수정: STATUS_LOCKED→TRADE_ALREADY_COMPLETED, 테스트 3개 추가)
- `carrot-web/src/lib/api/posts.ts` (수정: getPostBuyers, completePost 추가)
- `carrot-web/src/components/posts/TradeCompleteSheet.tsx` (신규)
- `carrot-web/src/app/posts/[id]/page.tsx` (수정: 거래완료 버튼 + TradeCompleteSheet)
- `carrot-mobile/lib/features/posts/data/posts_repository.dart` (수정: getPostBuyers, completePost 추가)
- `carrot-mobile/lib/shared/widgets/trade_complete_sheet.dart` (신규)
- `carrot-mobile/lib/features/posts/presentation/post_detail_screen.dart` (수정: 거래완료 버튼 + _showTradeCompleteSheet)
- `carrot-mobile/lib/core/api/api_endpoints.dart` (수정: postBuyers, postComplete 추가)

### Review Findings

- [x] [Review][Patch] complete_post — buyer_email을 실제 구매자 목록과 대조 안 함, 자기자신 지정 가능 [carrot-api/app/features/posts/service.py:complete_post]
- [x] [Review][Patch] CompletePostRequest.buyer_email 형식 검증 없음 [carrot-api/app/features/posts/schemas.py:CompletePostRequest]
- [x] [Review][Patch] Web — loading=false 후 animating 중 확정 버튼 재클릭 가능 [carrot-web/src/components/posts/TradeCompleteSheet.tsx:handleConfirm]
- [x] [Review][Patch] Flutter — completePost 성공 후 Future.delayed 동안 이중 탭 가능 [carrot-mobile/lib/shared/widgets/trade_complete_sheet.dart:_confirm]
- [x] [Review][Patch] Web — sheet 닫을 때 buyers 상태 리셋 안 됨 [TradeCompleteSheet.tsx:useEffect]
- [x] [Review][Patch] Web — getPostBuyers fetch 중 로딩 표시 없음 [TradeCompleteSheet.tsx]
- [x] [Review][Patch] Web — setTimeout cleanup on unmount 없음 [TradeCompleteSheet.tsx:handleConfirm]
- [x] [Review][Patch] get_post_buyers 서비스 단위 테스트 없음 [test_posts_service.py]
- [x] [Review][Defer] 동시 PATCH 경쟁 조건 [service.py] — deferred, SELECT FOR UPDATE 필요
- [x] [Review][Defer] 삭제된 계정 buyer_email 목록 노출 [repository.py] — deferred, FK 아키텍처 이슈
- [x] [Review][Defer] chat_count/like_count 하드코딩 [service.py] — deferred, 기존 패턴
- [x] [Review][Defer] PostDetailResponse에 buyer_email 미포함 [schemas.py] — deferred, 설계 결정 (클라이언트 재조회)

## Change Log

- **2026-05-30**: Story 4.2 생성 — 구매자 확정 API, TradeCompleteSheet(Web+Flutter), 체크 애니메이션, 후기 CTA
- **2026-05-30**: Story 4.2 구현 완료 — PATCH /complete 엔드포인트, GET /buyers, TradeCompleteSheet(Web+Flutter), 49/49 테스트 통과
